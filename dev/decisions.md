- 2026-09-17 D1 track `dev/` in Git and keep machine-specific environments, preference files and local stack scripts ignored - why: `archive/dev_distributed/` was the only copy of an eight-phase record, while `dev/petscenv*/LocalPreferences.toml` carries absolute paths to one machine's custom builds
- 2026-09-17 D2 archive the motorBike record to `archive/dev_motorbike/` and the 2026-07 distributed polish round under `archive/dev_distributed/`, then scaffold a fresh vault rather than migrating in place - why: one `dev/` held two features and `migrate` would have adopted the closed motorBike phase as the live record
- 2026-09-17 D3 keep the repository's existing top-level `archive/` as the durable home for closed features and use the vault's own `dev/archive/` only for records of the active phase - why: `archive/dev_distributed/` is already tracked and cited by the previous round's close record
- 2026-09-17 D4 measure and develop against a stock-binary project environment holding only XCALibre, MPI and PETSc - why: it precompiled and ran the distributed backward-facing-step case with no preferences file and no shell configuration, so the custom stack is not needed for the CPU Float64 path
- 2026-09-17 D5 retire the `PETSC_OPTIONS` environment variable through `PETSc.initialize(petsclib; options, log_view)` rather than a wrapper - why: PETSc.jl already exposes both as keywords and sets the variable itself for the duration of initialisation
- 2026-09-17 D6 use `Cg` with `Jacobi` for the scaling attribution rather than an algebraic multigrid preconditioner - why: Jacobi is partition-invariant, and residuals at one and two ranks then agreed to thirteen significant figures, which removes hierarchy differences as an explanation for any timing gap
- 2026-09-17 D7 the distributed gate runs two ranks over halo, partition, assembly, laplace and psimple from the prescribed test command and skips itself where MPI or PETSc will not load - why: a gate that fails on machines without the stack would be disabled rather than fixed, and these five files cover partitioning, exchange, assembly and the coupled solver path
- 2026-09-17 D8 the clock-normalised scaling figures are an indicator consistent with throttling, not an attribution; P1-M3 closes only on a PETSc stage split and a heated single-rank control - why: rank count and thermal load co-vary perfectly in the run, the correction assumes CPU-bound work, and 103% efficiency at two ranks is that assumption over-correcting
- 2026-09-17 D9 the distributed scaling loss on this machine is roughly two thirds core throttling and one third parallel cost - why: one rank with five cores held busy by spin loops costs 2.807 s/iter against 1.998 idle, so efficiency at six ranks is 70% against the heated baseline and 50% against the idle one (dev/telemetry/scaling_bfs_cg_jacobi.md)
- 2026-09-17 D10 the preconditioner is not part of the scaling loss - why: Cg with Jacobi reproduces the 2026-07 Cg with GAMG efficiency curve on the same case to within three points at every rank count
- 2026-09-17 D11 remove the three `XCALIBRE_AMG_DEVICE_COARSE_*` environment variables rather than exposing them - why: they route into a device Krylov coarse solve only under `mode=Cg()`, the one combination the AMG constructor rejects, and `coarse_solve=OnDeviceKrylov()` already exposes the sound form
- 2026-09-17 D12 XCALibre is 4.9x faster per iteration than OpenFOAM at one rank on the same 500k-cell case and still 3.0x faster at eight, but its parallel efficiency at eight ranks is 47% against OpenFOAM's 76% - why: same mesh, session, binding and solver settings (dev/telemetry/scaling_bfs_cg_jacobi.md); a fixed communication cost is a larger share of a 174 ms iteration than of a 526 ms one, but that cannot account for 29 points
- 2026-09-17 D13 P1-M5 measures what MPI and Metis cost a serial user before any extension refactor - why: `HaloExchange` names `MPI.Comm` and `Vector{MPI.Request}` in its fields and every file in the module calls MPI, so the refactor is six steps, and a cost of a fraction of a second would not justify it
- 2026-09-17 D14 the distributed scaling loss is entirely inside the PETSc Krylov solve; XCALibre's own kernels scale superlinearly - why: at 5 mm, `KSPSolve` goes 0.203 to 0.151 s/iter from two to eight ranks (34% efficiency) while everything else goes 0.253 to 0.057 (111%), from `-log_view` (dev/telemetry/scaling_bfs_cg_jacobi.md)
- 2026-09-17 D15 SUPERSEDED BY D16 - proposed per-rank communication-volume imbalance as the proximate cause - why: owned cells are equal to 1.00 at every rank count while ghost cells reach 3.63 max/min at eight ranks, matching the 3.566 message-length imbalance PETSc reports and the 38x wait at `VecNorm`
- 2026-09-17 D16 SUPERSEDES the mechanism in D15: the scaling ceiling is memory bandwidth, not communication imbalance - why: `VecAXPY`, which communicates nothing and is balanced to 1.1, scales at only 63% from two to eight ranks with its work reduced exactly fourfold, and 19 kB against 5 kB of halo cannot produce a millisecond of all-reduce wait
- 2026-09-17 D17 no Metis objective is worth changing for this: volume cuts total communication 11% and leaves the per-rank imbalance at 3.66 against 3.63 - why: the imbalance is the domain's shape, end subdomains having fewer neighbours than interior ones, and it is not the mechanism anyway (D16)
- 2026-09-17 D18 MPI and Metis stay hard dependencies, with the cost recorded rather than removed - why: they cost a serial user 0.17 s of an 0.809 s package load and 27 MB of artifacts, against a six-step refactor that would parameterise `HaloExchange` over MPI's types and move every body in the module to an extension (dev/telemetry/serial_cost_of_mpi_metis.md)
- 2026-09-17 D19 SUPERSEDES D16: the scaling ceiling is not memory bandwidth and the mechanism in D16 is withdrawn - why: `VecAXPY` reaches 104.6 GB/s at two ranks against this machine's 89.6 GB/s theoretical DRAM peak and a measured 110.3 GB/s cache-resident ceiling, so it never reads main memory and already runs at 95% of what the hardware allows; the 63% figure in D16 was never divided against the machine's peak (dev/telemetry/scaling_attribution.md)
- 2026-09-17 D20 the strong-scaling loss to four ranks is CPU frequency throttling, not the distributed module - why: with the clock pinned at 2200 MHz (`no_turbo=1`) efficiency is 102/94/71% at n=2/4/8 on 499,503 cells and 101/93/78% at n=2/4/6 on 1,320,368 cells, against 87/73/47% and 85/68/52% unpinned, where cores fall from 4400 to 3100 MHz as rank count rises
- 2026-09-17 D21 mesh size does not change strong-scaling efficiency, so the loss is a fixed fraction of the work - why: a 2.6x larger mesh gives 101/93% at n=2/4 against 102/94%, refuting both a communication-bound loss, which would improve with volume-to-surface ratio, and a DRAM-bandwidth loss, which would worsen with a 32 MB working set against 12 MB
- 2026-09-17 D22 WITHDRAWS the earlier OpenFOAM efficiency comparison and replaces it - why: it read OF clocks sampled after `foamRun` exited, so idle cores at 2000-2271 MHz, and set XCALibre's Cg+Jacobi against OF's PCG+diagonal rather than OF's production GAMG; at a pinned clock XCALibre ties OF GAMG at one rank, 1.0665 against 1.0620 s/iter, and scales better at every rank count, 102/94/71% against 95/79/62%
- 2026-09-17 D23 the residual loss above four ranks is reduction cost and is deferred, not closed - why: `VecNorm` is 53% of the eight-rank solve against 2% at two ranks with a 38.2x rank imbalance, and excluding it the solve scales at 71% rather than 34%
- 2026-09-17 D24 `test/Project.toml` must declare `Random` - why: `test_halo.jl` uses it and `Pkg.test` resolves exactly the declared dependencies, so the distributed gate failed under the repository's own test command while passing standalone from a richer environment
- 2026-09-18 D25 there is NO regression from six to eight ranks; the degradation is gradual from four ranks on - why: incremental efficiency per added rank is 102/93/87/86% at 1->2/2->4/4->6/6->8 on 499,503 cells and 101/92/84/88% on 1,320,368, so the 6->8 step costs the same as the 4->6 step on both meshes and full core occupancy shows no step change
- 2026-09-18 D26 BoomerAMG does not help this case and is not the distributed default - why: 0.2744 s/iter against Jacobi's 0.1877 at eight ranks, 46% slower, with a scaling curve no flatter (70.0% against 71.0%); 500k cells of laminar BFS is too well conditioned for the setup to amortise, and this was measured WITH hierarchy reuse active
- 2026-09-18 D27 pipelined CG does not recover the high-rank loss - why: 0.2200 s/iter against 0.1877 at eight ranks, 17% slower, with `KSP=pipecg` confirmed in the solver log; the extra per-iteration work needed to enable the reduction overlap exceeds the barrier wait it hides at this scale
- 2026-09-18 D28 `BoomerAMG(reuse=N)` FREEZES the preconditioner, it does not update coefficients - why: `KSPSetReusePreconditioner(PETSC_TRUE)` skips `PCSetUp` entirely, so N-1 solves apply a stale hierarchy and the Nth does a full hypre rebuild; freezing is worth 2x (0.8416 s/iter at reuse=1 against 0.4216 at reuse=10, n=4) and the optimum is near reuse=25 at 0.3951, so the default leaves about 6% unclaimed
- 2026-09-18 D29 GAMG, not BoomerAMG, is the preconditioner whose update semantics match a fixed mesh - why: `reuse_interpolation=true` keeps aggregation and prolongation and recomputes only RAP and the smoothers from the current matrix, which is the coefficient-only update a non-refining mesh permits, whereas PETSc's PCHYPRE exposes only freeze or full rebuild
- 2026-09-18 D30 our AMG is the weak configuration, not the distributed module - why: OpenFOAM's GAMG beats XCALibre's BoomerAMG by 1.24x to 1.45x at every rank count, while XCALibre's Cg+Jacobi beats OpenFOAM's GAMG by 6% to 16% at two ranks and above, so best-config against best-config XCALibre leads and AMG-against-AMG it does not
- 2026-09-18 D31 GAMG has the best scaling curve of every configuration tested and costs the same as BoomerAMG - why: 102.6/97.4/85.6/72.8% at n=2/4/6/8 against BoomerAMG's 96.7/92.6/75.6/70.0 and Jacobi's 101.6/94.5/82.6/71.0, at 0.2749 s/iter against BoomerAMG's 0.2744 at eight ranks; it is still 1.46x slower in absolute terms than Cg+Jacobi on this case
- 2026-09-18 D32 the coefficient-only update is worth 2.69x, more than BoomerAMG's freeze buys, and without the staleness - why: GAMG costs 0.4108 s/iter at four ranks with `reuse_interpolation` on and 1.1060 with it off, against BoomerAMG's 2.0x from freezing (0.8416 at reuse=1 to 0.4216 at reuse=10), so keeping the hierarchy and recomputing RAP beats both rebuilding and freezing
- 2026-09-18 D33 AMG breaks rank invariance and Jacobi does not, which is a documented consequence rather than a defect - why: after 100 SIMPLE iterations the pressure residual spans 2.4x across rank counts under BoomerAMG (7.85e-5 to 1.89e-4) and 0.3% under GAMG, against agreement to 15 significant figures under Jacobi, because an AMG hierarchy is built from the local partition and the partition changes with rank count
- 2026-09-18 D34 OpenFOAM's GAMG beats ours because our SETUP dominates, not our cycles - why: at one rank `PCSetUp` costs 26.39 s against `PCApply`'s 9.23 s, 2.9x, with `MatPtAPNumeric` alone at 12.11 s; PETSc GAMG forms Galerkin coarse operators by sparse triple product every solve while OpenFOAM's agglomeration multigrid sums fine coefficients into agglomerated cells with cached addressing, so their whole solve costs less than our setup, even though we converge in 2-3 iterations against their 6.7
- 2026-09-18 D35 stacking the freeze on the coefficient update closes the gap to OpenFOAM - why: `GAMG(reuse=25)` gives 1.1065/0.3023/0.2115 s/iter at n=1/4/8 against OpenFOAM GAMG's 1.0620/0.3354/0.2127, so we lead by 1.11x at four ranks, tie at eight and trail 4% at one; the freeze is worth 1.30x to 1.42x because it skips the `PCSetUp` that was 31% of runtime
- 2026-09-18 D36 GAMG keeps its rank invariance under the freeze, unlike BoomerAMG - why: the pressure residual spread across rank counts is 0.50% with `reuse=25` against 0.3% unfrozen and 240% for BoomerAMG, so the freeze costs almost nothing in reproducibility here
- 2026-09-18 D37 `Cg()+Jacobi()` remains the fastest configuration on this case and stays the documented default; `GAMG(reuse=25)` is the AMG to recommend - why: Jacobi is 0.1877 s/iter at eight ranks against frozen GAMG's 0.2115, 11% faster, and 500k cells of laminar BFS is too well conditioned for any AMG setup to amortise; AMG's case must be made on a stiffer problem
- 2026-09-18 D38 `reuse` on `BoomerAMG`/`GAMG` is renamed `freeze`, with `reuse` kept as a deprecated alias for one release - why: the count sets `KSPSetReusePreconditioner`, which skips `PCSetUp` for N-1 solves (D28), so the preconditioner is frozen rather than updated; "freeze" also avoids clashing with GAMG's `reuse_interpolation`, which is the coefficient update
- 2026-09-18 D39 `GAMG` freeze default moves 1 -> 25; `BoomerAMG` stays at 10 - why: GAMG final p residual at n=4 after 100 iters is 2.953e-4/2.950e-4/2.954e-4 at freeze 1/10/25 (`dev/telemetry/freeze_residuals.md`) so the pinned 1.36x (D35) is free, while BoomerAMG freeze 25 ends at 1.85e-4 against 7.83e-5 at 10, 2.36x, outside S2's 2x band
- 2026-09-18 D40 the AMG recommendation moves from `BoomerAMG` to `GAMG` and the rank-invariance caveat is documented in both docstrings and the user guide - why: GAMG at its best freeze is faster than BoomerAMG at its best at every rank count (0.3023 vs 0.3951 s/iter at n=4, `dev/telemetry/scaling.csv`) and its residual spread across ranks is 0.50% against 2.4x (D36)
- 2026-09-18 D41 S1 amended: the `reuse` deprecation alias is removed, `freeze` is the only keyword - why: `BoomerAMG`/`GAMG` are unreleased (added under v0.6.1-DEV in CHANGELOG.md, absent from origin/main and every tag), so the alias protects no user and is dead code
- 2026-09-18 D42 the PETSc path no longer substitutes `convergence` for `atol` when `atol`=`rtol`=0; `convergence` is the outer-loop target only, as in serial - why: the outer loop already checks it on the Allreduced residual (`Solvers_1_SIMPLE.jl` convergence check), Krylov runs to itmax at zero tolerances, and PETSc at itmax=1 does not throw (P1-M8 probe), so the substitution was the only divergence.
- 2026-09-18 D43 the `wall_distance!` non-convergence note is withdrawn from docs and roadmap - why: its threshold is the user's `y` `convergence` (`Calculate_4_wall_distance.jl`); the 1e-15 came from the distributed test harness, which sets unreachable targets by design (`test/distributed/psimple_case.jl` header).
- 2026-09-18 D44 PETSc mapping gains `Cgs`->cgs and `DILU`->bjacobi (ILU(0) blocks); unmapped types defer to `petsc_options` and error only if it names none; no other KSP/PC gets a type - why: `DILU` is in 11 example/test setups and errored distributed; the passthrough the error advertised was unreachable (type error fired before options merged); asm/sor/pipelined types lack a measured win (D26, D27) and stay passthrough-only.
- 2026-09-18 D45 single-process PETSc is documented as `distribute` under plain `julia`, not built as a serial-`Mesh` PETSc path - why: 2D BFS 20 iters ran in one process via MPI singleton with Jacobi and GAMG (P1-M8 probe); a serial-mesh path would duplicate `wrap_eqn`/`residual`/`setReference!` dispatch for no measured demand.
- 2026-09-18 D46 the distributed user guide states mechanisms only and its API runs as doctests at one rank (PETSc added to the docs env); measured figures leave the guide - why: user request; case-specific numbers aged within the phase (D16 withdrawn by D19).
- 2026-09-18 D47 GPU fields with a PETSc lacking the device package always error; the `solve_on=CPU()` host-copy opt-in is removed (P1-M9), and a device-resident path without per-solve host staging is built in P1 (P1-M10) - why: user ruling that a GPU run must not be forced onto the host; `passemble!`/`psolve!` stage A, b and x through host vectors on every path today.
- 2026-09-18 D48 P1 defers nothing: every item flagged for later (D23 reductions, D26 AMG retest, freeze-vs-mesh-size) becomes a P1 milestone (P1-M11, P1-M12) - why: user ruling that the release-polish phase carries no deferred work.
- 2026-09-18 D49 distributed LKE/LES, Float32 hypre, multi-GPU scaling validation and AMD parity stay non-requirements of P1 and are not deferred work - why: user ruling that they are real non-goals.
- 2026-09-18 D50 `DILU` keeps its PETSc block-ILU(0) mapping but warns once that it is a substitute (added to P1-M9) - why: user ruling; the mapping is a different preconditioner and must not be silent (D44).
- 2026-09-18 D51 PETSc assembly is COO (`MatSetPreallocationCOO` once, `MatSetValuesCOO` on the nzval pointer per solve) and vectors copy through PETSc.jl's device-aware `withlocalarray!`, with a device-wide sync either side of the solve; host staging buffers and the in-place `MatConvert` are gone - why: GPU 0.097 to 0.073 s/iter on BFS 5 mm n=1, CPU n=2 median 0.457 to 0.445 (noise), residuals unchanged (`dev/telemetry/device_resident_petsc.md`).
- 2026-09-18 D52 distributed PETSc solves on AMD GPUs error at setup rather than run - why: PETSc.jl ships a CUDA memory backend only, so a HIP Vec cannot be handed back as a ROCArray and the device-resident path (D51) cannot run there; AMD parity is a non-goal (D49).
- 2026-09-18 D53 every PETSc KSP takes `rtol` relative to the warm-started initial residual (`KSPConvergedDefaultSetUIRNorm`), as Krylov.jl does; D42's claim that tolerances already matched was wrong for rtol's reference - why: with PETSc's default right-hand-side reference the distributed BFS ended 30 iterations 10-30x above serial (p 1.10e-3 vs 1.09e-4), with it 1.24e-4; PETSc computes the default reference in the running norm (iterativ.c:1541-1572), so this is independent of D54 (`dev/telemetry/tolerance_semantics_and_reductions.md`).
- 2026-09-18 D54 a resolved `cg` KSP uses the natural norm unless the user names one - why: Krylov.jl CG stops on sqrt(r'Mr) (cg.jl:164); drops one reduction per CG iteration (VecNorm 1325 to 120 at n=2); keyed on the merged type so a passthrough -ksp_type never inherits it.
- 2026-09-18 D55 the eight-rank loss is not the reduction COUNT, so D23's framing is restated: cutting ~3.1 to ~2.1 reductions per CG iteration left the n=2 to n=8 speedup at 2.83 against 2.88 - why: equal-thermal runs in `dev/telemetry/tolerance_semantics_and_reductions.md`; the next candidates are single-reduction CG (one screen) and the MatMult/ghost imbalance behind the VecNorm wait (D15).
- 2026-09-18 D56 P1-M12 is superseded by P1-M13: BoomerAMG stays exposed, and the milestone characterises every distributed preconditioner for users and sets defaults from measurement instead of deciding keep-or-drop - why: user ruling that keeping BoomerAMG costs little and what users need is what each solver does, does not, and sensible defaults.
- 2026-09-18 D57 P1-M13 draws preconditioner behaviour from published sources (PETSc/hypre manuals and papers, CFD solver documentation) and runs only the XCALibre measurements needed to confirm or contradict them on this framework - why: user ruling; literature already answers the general questions at no run cost.
- 2026-09-18 D58 single-reduction CG (`-ksp_cg_single_reduction`) is refused - why: equal-thermal n=8 0.268/0.272 s/iter against 0.231/0.235 without it, n=2 unchanged within noise (`dev/telemetry/tolerance_semantics_and_reductions.md`).
- 2026-09-18 D59 the eight-rank wait is inside the PETSc solve, not in XCALibre's work before it: ranks reach each assembly within 0.022-0.153 s total over 23 outer iterations, while -log_sync shows KSPSolve at 1.55 s of balanced work against 4.25 s unsynchronised, with VecScatterEnd at ratio 30 - why: `scaling_probe.jl wait=1` and `-log_sync` at n=8 (`dev/telemetry/tolerance_semantics_and_reductions.md`); ghost cells per rank span 654-2373, so halo exchange is the next suspect.
- 2026-09-18 D60 P1-M11 closes as delivered and the index-width mechanism opens as P1-M14 - why: the stock PETSc_jll Float64 list is Int64 first, so `_petsclib` picks 64-bit indices (16 bytes per nonzero against 12) while MatMult is the memory-bound kernel (scaling_attribution.md); it acts at every rank count, so it is not the reduction-count milestone's mechanism.
- 2026-09-18 D61 PETSc takes the narrowest-index library of the field precision that addresses the global nonzero count, chosen from an all-reduced count so every rank agrees - why: 32-bit indices cut per-iteration time 5% at n=2 and 12% at n=8 with bitwise-identical residuals (`dev/telemetry/petsc_index_width.md`); both stock Float64 libraries carry hypre, so no capability is lost.
- 2026-09-18 D62 P1-M13 maps `IC0GPU` to block-Jacobi ICC(0) and `ILU0GPU` to block-Jacobi ILU(0) with the DILU-style substitution warning, documents ASM, SOR and HPDDM as `petsc_options` recipes, and measures GPU BoomerAMG on the local CUDA-hypre PETSc; AmgX is out (not in the local build) and distributing XCALibre's native AMG is a separate project outside P1 - why: user ruling; the local build has PETSC_HAVE_CUDA, PETSC_HAVE_HYPRE and HYPRE_USING_CUDA, no AmgX.
- 2026-09-18 D63 BoomerAMG defaults gain `P_max=4` (hypre's recommendation with ext+i); declaring pure-Laplacian matrices SPD is withdrawn - why: BoomerAMG 0.652 to 0.639 s/iter with p residual 6.19e-5 to 6.25e-5; the SPD flag left GAMG residuals bitwise identical and time within noise, so it bought nothing measurable (`dev/telemetry/preconditioner_guidance.md`).
- 2026-09-18 D64 `IC0GPU` and `ILU0GPU` run distributed as per-rank block ICC(0) and ILU(0), each warning once per type - why: both completed the 5 mm n=2 case (p 9.94e-5 at 30 iterations against Jacobi's 1.24e-4, `dev/telemetry/preconditioner_guidance.md`); D62 ruling.
- 2026-09-18 D65 defaults stay (GAMG freeze 25, BoomerAMG freeze 10, Jacobi for momentum) and the guide recommends Jacobi for small partitions, GAMG for large pressure meshes and on the GPU, BoomerAMG where GAMG converges poorly; AMG costs roughly a third more memory per rank - why: S2/S5 in `dev/telemetry/preconditioner_guidance.md` agree with the literature: Jacobi x3.73 vs GAMG x3.13 for x2.64 cells, AMG gain gone at ~62k cells per rank, GPU GAMG 0.059 vs Jacobi 0.075 s/iter, peak RSS per rank at 4 mm n=2 Jacobi 2.73, BoomerAMG 3.48, GAMG 3.69 GB.
- 2026-09-18 D66 `petsc_options` also takes a named tuple keyed by `all` and the equation labels, with unknown keys an error - why: a String reaches every solve, so ASM for momentum and GAMG for pressure could not be combined; PETSc option prefixes would have meant renaming every curated option key.
- 2026-09-18 D67 P1 gains P1-M15 (example comment blocks to the three-line limit) and P1-M16 (memory footprint: measured breakdown first, then cures); memory cures touching code outside the distributed work go on one branch off main as a single memory PR, not one PR per cure, while distributed-only cures land on HM/distributed-draft - why: user ruling after the 4 mm BFS at 8 ranks with AMG exhausted this 14 GB machine, with about 4 KB per cell before PETSc.
- 2026-09-18 D68 P1 gains P1-M17 (MPI/PETSc setup guide, including the CUDA route), ordered before P1-M15 and P1-M16 - why: user ruling; users compiling PETSc is the main barrier to GPU runs, and the page tells them only that preferences exist.
- 2026-09-18 D69 The setup guide leads its GPU route with the conda-forge `petsc=*=cuda12_real*` build (no compiling), OpenMPI variant preferred as CUDA-aware, MPICH variant with `-use_gpu_aware_mpi 0`; compiling PETSc becomes the fallback - why: both variants pass `test_gpu.jl` at n=1,2 on the device, deltas 1e-11 vs serial (`dev/telemetry/conda_cuda_petsc.md`)
- 2026-09-18 D70 The guide tells GPU users of the conda build to use `GAMG()` or `Jacobi()` for pressure and states that `BoomerAMG()` crashes there; turning that crash into an error is left for a separate milestone, not P1-M17 - why: BoomerAMG with device fields segfaults at n=1,2 on host-only hypre while GAMG passes (`dev/telemetry/conda_cuda_petsc.md`); PETSc exposes no runtime query for hypre's device support, so the fix needs its own design
- 2026-09-18 D71 Distributed GPU solves pick the communication path from `MPI.has_cuda()`: direct when CUDA-aware (one `@info`), otherwise PETSc's exported `use_gpu_aware_mpi` flag is cleared so messages stage through the host (one `@warn`); an explicit `-use_gpu_aware_mpi` in `petsc_options` wins - why: user ruling; R2 forbids needing an env var, PETSc aborted at first device use on both conda MPIs without one, and a start-up option is lost when PETSc was initialised earlier (`dev/telemetry/conda_cuda_petsc.md`)
- 2026-09-18 D72 `AUDIT.md` reshapes the rest of P1: milestones P1-M18 to P1-M24 (release blockers, gate widening, O(N+P) extraction, owned-row single-copy system, fused communication, parallel preprocessing, binary I/O with restart) and the outcome gains "structurally ready to scale"; spec gains R11 and R12 - why: the audit found the module correct within 8 ranks and one GPU but capped by rank-0 O(P·N) preprocessing, 3.5-4x matrix multiplicity at 4.1 KB/cell peak, and 8 blocking exchange rounds plus 12 device drains per SIMPLE iteration (`AUDIT.md`).
- 2026-09-18 D73 multi-node, multi-GPU and AMD validation is phase P2 on the HPC, after M18-M24 land; P1 takes what is correct and performant on this machine (n=1-8 CPU, n=1-2 on one GPU) as sufficient - why: user ruling; no such hardware here, and every M18-M24 verdict is measurable locally.
- 2026-09-18 D74 the owned-row CSR enters through one mesh seam (`n_rows(mesh)` = `length(mesh.cells)` serial, `n_owned` distributed) so shared kernels change only where their row range comes from; the shared-code memory cures (sparsity shared across equations, preallocated connectivity, no per-equation host mesh copy) stay on D67's single PR off `main` - why: keeps D67 while letting the distributed cure land on this branch with serial code paths unchanged.
- 2026-09-18 D75 M18-M24 are implemented by Fable (claude-fable-5-1) in ONE fresh session, in `dev/phaseRoadmap.md` order, landing every step and not stopping at milestone boundaries - why: user ruling; the plans carry mechanism, cost and verdict per step, which is what a one-shot session needs.
- 2026-09-18 D76 P1-M16 is restated as the memory breakdown only (its S1); its cures are P1-M21's steps and the D67 PR - why: the audit split memory into four sources (rank-0 mesh, matrix multiplicity, setup transients, GC per rank), each with its own cure and owner (`AUDIT.md` § Memory).
- 2026-09-18 D77 `AUDIT.md` moves to `dev/archive/reviews/p1/audit-2026-09-18.md` when P1-M18 closes, and the plans cite that path - why: the audit itself lists stray root-level documents as a release blocker.
- 2026-09-19 D78 partition files start with a plain-text header (format, julia, xcalibre, nranks, TI, TF) checked before anything is deserialised, with exact version match required and `distribute(reader; dir)` regenerating on mismatch - why: `.jls` parts failed after upgrades deep inside the solver (audit blocker 6); a text header needs no serializer compatibility to be read.
- 2026-09-19 D79 the BoomerAMG GPU guard asks hypre for its execution policy (`HYPRE_GetExecutionPolicy` == device), not its memory location, and `BoomerAMG(device=true)` skips it - why: measured 2026-09-19 on the conda host-only and the custom CUDA hypre: memory location reports device (1) on BOTH builds because a CPU hypre maps device memory to host, execution policy reports 0 vs 1.
- 2026-09-19 D80 `initialise_writer(VTK, DistributedMesh)` returns a sentinel writer whose `write_results` errors, rather than erroring at setup - why: the writer is built before `write_interval` is known and VTK is the default output, so a setup error would break every 2D distributed run that writes nothing; `save_output` dispatches on the physics type, so the hook is `write_results`.
- 2026-09-19 D81 `DistributedMesh` stores its communicator and every exchange, reduction, solver and gather defaults to it; a part received over MPI or loaded from disk is re-stamped with the caller's comm - why: `MPI.Comm` serialises as a raw handle (an Open MPI pointer) that means nothing on another rank, checked 2026-09-19.
- 2026-09-19 D82 `bind_device!(backend)` binds by node-local rank from `Comm_split_type(COMM_TYPE_SHARED)` and warns once when a node's ranks exceed its devices; `bind_device!(backend, rank)` stays for explicit control - why: global-rank binding is wrong whenever ranks do not fill nodes contiguously with one rank per GPU (audit blocker 4).
- 2026-09-19 D83 the hypre execution-policy query initialises hypre first when `HYPRE_Initialized` is false - why: querying an uninitialised hypre 3.1 creates its handle and PETSc's later `HYPRE_BoomerAMGCreate` then fails with error code 1 (stock env, `test_hypre.jl` n=1, 2026-09-19); PETSc itself calls `HYPRE_Initialize` before creating the preconditioner.
- 2026-09-19 D84 P1-M18-S2 to S10 land as one commit under one CPU verdict (suite 12/12 at n=1,2), with the GPU verdicts of S2, S6 and S10 taken by the S11 runs - why: nine small edits sharing files, and the GPU test could not run to its assertions until the S11 segfault was reproduced and understood.
- 2026-09-19 D85 the PETSc extension snapshots the eleven signal dispositions PETSc resets and restores them after building a solver, instead of a launch flag or thread setting - why: PETSc's CUDA device init pops an empty signal-handler stack and leaves SIGSEGV at SIG_DFL, so Julia's safepoint faults on the interactive thread killed 3 of 15 GPU runs silently; restored, 26/26 clean and `test_gpu.jl` green at n=1,2 on both CUDA envs (`dev/telemetry/conda_cuda_petsc.md`); R2 forbids the `-use_gpu_aware_mpi 0` or `-t 1,0` workarounds.
- 2026-09-19 D86 `extract_subdomain` reads from a `_PartIndex` built once per decomposition (cells, interior faces and boundary faces bucketed per part in one pass; reset-on-exit `Vector{TI}` inverse maps; the dual graph taken from `faces_range`/`cell_neighbours` with rows sorted and deduplicated) - why: parts bitwise identical on all three configurations; `decompose` 1.77 to 0.97 s at 5 mm P=64, 0.82 to 0.58 s at P=8 (`dev/telemetry/extraction_cost.md`); the 5x the plan asked for was not there to take, since the P·N term was 1 s of 1.77 at this size.
- 2026-09-19 D87 the communication budget is asserted per SIMPLE iteration as 7 exchanges and 6 all-reduces on 2D meshes (8 and 8 in 3D) from global counters in the halo exchange and the solver seams; `runtests_mpi.jl` defaults to ranks 1,2,3,5 and the gate runs at 2 and 3 - why: the 2D BFS has two momentum components, so the audit's 8/8 is the 3D figure; gate 10/10 in 196 s against Q2's 300 s.
- 2026-09-19 D88 the distributed driver wraps its (file, rank) loop in one outer testset - why: a top-level for-testset throws at the end of the first failing iteration, so one failing file silently skipped every later file and rank count (seen twice on 2026-09-19).
- 2026-09-19 D89 `test_invariance.jl` holds residual histories across rank counts to a relative 1e-4 per iteration (Q1), not the plan's 1e-12 - why: n=2,3,5 against n=1 measured 2.0e-6, 2.7e-6, 2.9e-6 max relative spread over 50 iterations (BFS, Jacobi, `rtol=1e-8`); PETSc's parallel dot products change the reduction order, so bitwise histories were never on offer.
- 2026-09-19 D90 P1-M16 delivered: the per-rank memory baseline is 1950 MB peak at 4 mm n=2 (about 790 MB fixed + 1.85 KB per local cell), replacing 2.79 GB, which was three `run!` setups in one process; the fixed runtime and compiled code are the largest item below ~440k cells per rank and no planned cure touches them - why: `dev/telemetry/memory_breakdown.md`; same parts through `scaling_probe.jl` peak at 2838 MB.
- 2026-09-19 D91 P1-M21-S2 is absorbed by P1-M16 and P1-M21-S3 (no COO map on host matrices) goes ahead - why: PETSc's two 8-byte-per-nonzero COO arrays are 99.2 of its 290 MB per rank at 4 mm n=2, 34 percent against the 15 percent bar (`dev/telemetry/memory_breakdown/petsc_sites_4mm_rank0.tab`).
- 2026-09-19 D92 P1-M21-S1 (owned-row CSR through `n_rows`) is DEFERRED TO P1-M22 as a kernel-range step, its rectangular-CSR half only if M22's timing shows ghost rows matter; D74's ordering of S1 first is amended, since S3 and S4 need only the owned CSR prefix, which exists today - why: ghost rows are 0.46/2.94/10.77 percent of local rows at n=2/8/64 on the 5 mm BFS (`dev/telemetry/memory_breakdown.md`), about 1 MB of 205 MB equation storage at the M21 measurement point.
- 2026-09-19 D93 P1-M21-S3 fills PETSc's diagonal and off-diagonal SeqAIJ blocks directly from the local CSR after one `MatCreateMPIAIJWithArrays`, not through `MatUpdateMPIAIJWithArray` - why: PETSc 3.24 `mpiaij.c:4305` requires values in global-sorted column order per row, while the local CSR lists owned columns then ghosts; ghosts are sorted by (owning rank, id) (`Distribute_1_partition.jl:202`), so each segment already matches its block and no map is needed.
- 2026-09-19 D94 P1-M21-S3 landed: host PETSc matrices carry no COO map - why: PETSc heap 290 → 206 MB and natural peak 1950 → 1793 MB per rank at 4 mm n=2; 20-iteration residual hashes bitwise equal at n=2 and n=8; gate 10/10 and test_periodic/perf/invariance/ghosts green at n=2,3 (`dev/telemetry/memory_breakdown.md`).
- 2026-09-19 D95 P1-M21-S4 landed: PETSc x and b own no storage and borrow the field's owned prefix and the equation's b per solve (`Vec(CUDA)PlaceArray`); `test_perf.jl` warms each measured call once before measuring - why: steady-state allocations at n=2 psolve! 3136 → 1664 B and passemble! 6448 → 4880 B, residual hashes bitwise equal at n=2 and n=8, gate 10/10, `test_gpu.jl` n=1,2 green; the test had been measuring a first call's thunk compilation (psolve! 8416 B first call, 1664 B after).
- 2026-09-19 D96 the per-rank heap recipe is `--heap-size-hint` of about node memory over ranks per node, after a plain-session precompile, documented in the distributed guide - why: 4 mm BFS n=2 at 1200M peaks 1667/1663 MB against 1772/1759 MB without it, residual hash unchanged; ranks loaded existing caches under the flag, so the earlier cache-flag failure applies only to a rank that must precompile.
- 2026-09-19 D97 REFUSED: `GC.gc(true)` before each distributed solver is built - why: 4 mm BFS n=2 peak 1773/1759 MB with it against 1772/1747 MB without; the high-water mark is set during the iterations, not at setup, and freed Julia memory is not returned to the OS.
- 2026-09-19 D98 each process keeps one live PETSc solver per equation label and destroys it when the label is wrapped again, so repeated `run!` calls no longer leak - why: PETSc heap after three extra `run!` calls at 5 mm n=2 went 70/140/210/280 MB before, 70/70/70/70 MB after; labels are unique within a run and every rank wraps in the same order, so the collective destroys match; a finalizer would run at different times on different ranks.
- 2026-09-19 D99 P1-M21 closes as delivered with S1 deferred to P1-M22-S8 (D92) and the time band measured at n=2 and n=4 instead of n=8 - why: peak per rank 1950 → 1772 MB at 4 mm n=2 and 1202 → 1152 MB at 5 mm, per-iteration time inside the 3 percent band at n=2 and n=4 and on the GPU, CPU residuals bitwise equal; n=8 at 5 mm needs about 7 GB beside the 2.8 GB language server and the memory guard stopped it.
- 2026-09-19 D100 P1-M25 (per-rank fixed memory) is opened and ordered before P1-M22 - why: user ruling; the fixed cost is about 790 MB per rank, 41 percent of the 4 mm n=2 peak and more than the whole per-cell cost below about 440k cells per rank (`dev/telemetry/memory_breakdown.md`), and no other milestone touches it.
- 2026-09-19 D101 every gate and experiment run whose result decides a verdict finishes within five minutes of wall clock; a verification set that would exceed it is cut to what the change can reach, or the case is made smaller or faster, never run longer - why: user ruling; step verifications of 20+ minutes (suite batches, a four-version timing sweep) made three trials cost an hour.
- 2026-09-19 D102 P1-M25-S1 delivered: fixed per-rank cost splits into about 250 MB shared per node and 324 MB private per rank after load (n=4, 10 mm); first-run growth is GC heap and malloc, JIT code only 2 MB; removable package rows are PETSc.jl image 47 private and Pkg 17 - why: `dev/telemetry/fixed_rank_memory.md`, runs 16 s and 20 s wall
- 2026-09-19 D103 P1-M25-S2 delivered: removable private MB per rank ranks first-run compilation (about 190 of 229 growth: GC live +27 only, second run! +21, malloc_trim returns 28) then PETSc.jl +102 over XCALibre+MPI (ten libpetsc variants dlopened, 87 MB image); Pkg arrives via MPI and LLVMExtra_jll so is not removable here; GPUArrays/LLVM 70-95 is a direct dependency, out of scope - why: `dev/telemetry/fixed_rank_memory.md` § S2
- 2026-09-19 D104 the first-run private growth is GC page retention, not compiler memory, and the M25 exit baseline is private at `iterations` rank 0 10 mm n=4 gc=0 = 556 MB (hash dbc3c69ab48b3394); the PETSc.jl row is 47 MB private per rank at n=4, not 102 - why: GC bytes_resident 254 vs 51 live, `--heap-size-hint=250M` cuts private 556 → 390 with hash unchanged; telemetry § S2
- 2026-09-19 D105 XCALibre will not set a per-rank GC memory target at runtime (user option 1 refused on data); the heap-size hint stays a user launch flag documented in S5 - why: steady-state private 459 vs 472/500 baseline, +11-17 percent `run!` time, telemetry § S3
- 2026-09-19 D106 runtime compilation causes the first-run private growth: harvested precompile statements loaded before setup cut private at `iterations` 559 → 359 MB (−36 percent) and `t_iter_s` 11.46 → 0.04 at 10 mm n=4 with hash unchanged; 256 of 937 statements bake a rank-local kernel size, so harvested statements cannot ship as-is - why: telemetry § S4, `dev/telemetry/memory_breakdown/m25s4_*.txt`
- 2026-09-19 D107 precompiling only case-independent signatures gains nothing (private 572 vs 559, `t_iter_s` 11.15 vs 11.46), so a shippable precompile must cover the case-typed solver call tree and size-free kernels - why: `dev/telemetry/memory_breakdown/m25s4_generic.txt`
