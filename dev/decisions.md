- 2026-09-17 D1 track `dev/` in Git and keep machine-specific environments, preference files and local stack scripts ignored - why: `archive/dev_distributed/` was the only copy of an eight-phase record, while `dev/petscenv*/LocalPreferences.toml` carries absolute paths to one machine's custom builds
- 2026-09-17 D2 archive the motorBike record to `archive/dev_motorbike/` and the 2026-07 distributed polish round under `archive/dev_distributed/`, then scaffold a fresh vault rather than migrating in place - why: one `dev/` held two features and `migrate` would have adopted the closed motorBike phase as the live record
- 2026-09-17 D3 keep the repository's existing top-level `archive/` as the durable home for closed features and use the vault's own `dev/archive/` only for records of the active phase - why: `archive/dev_distributed/` is already tracked and cited by the previous round's close record
- 2026-09-17 D4 measure and develop against a stock-binary project environment holding only XCALibre, MPI and PETSc - why: it precompiled and ran the distributed backward-facing-step case with no preferences file and no shell configuration, so the custom stack is not needed for the CPU Float64 path
- 2026-09-17 D5 retire the `PETSC_OPTIONS` environment variable through `PETSc.initialize(petsclib; options, log_view)` rather than a wrapper - why: PETSc.jl already exposes both as keywords and sets the variable itself for the duration of initialisation
- 2026-09-17 D6 use `Cg` with `Jacobi` for the scaling attribution rather than an algebraic multigrid preconditioner - why: Jacobi is partition-invariant, and residuals at one and two ranks then agreed to thirteen significant figures, which removes hierarchy differences as an explanation for any timing gap
- 2026-09-17 D7 the distributed gate runs two ranks over halo, partition, assembly, laplace and psimple from the prescribed test command and skips itself where MPI or PETSc will not load - why: a gate that fails on machines without the stack would be disabled rather than fixed, and these five files cover partitioning, exchange, assembly and the coupled solver path
- 2026-09-17 D8 the clock-normalised scaling figures are an indicator consistent with throttling, not an attribution; P1-M3 closes only on a PETSc stage split and a heated single-rank control - why: rank count and thermal load co-vary perfectly in the run, the correction assumes CPU-bound work, and 103% efficiency at two ranks is that assumption over-correcting
- 2026-09-17 D9 the distributed scaling loss on this machine is roughly two thirds core throttling and one third parallel cost - why: one rank with five cores held busy by spin loops costs 2.807 s/iter against 1.998 idle, so efficiency at six ranks is 70% against the heated baseline and 50% against the idle one (dev/telemetry/scaling_bfs_cg_jacobi.md)
- 2026-09-17 D10 the preconditioner is not part of the scaling loss - why: Cg with Jacobi reproduces the 2026-07 Cg with GAMG efficiency curve on the same case to within three points at every rank count
- 2026-09-17 D11 remove the three `XCALIBRE_AMG_DEVICE_COARSE_*` environment variables rather than exposing them - why: they route into a device Krylov coarse solve only under `mode=Cg()`, the one combination the AMG constructor rejects, and `coarse_solve=OnDeviceKrylov()` already exposes the sound form
- 2026-09-17 D12 XCALibre is 4.9x faster per iteration than OpenFOAM at one rank on the same 500k-cell case and still 3.0x faster at eight, but its parallel efficiency at eight ranks is 47% against OpenFOAM's 76% - why: same mesh, session, binding and solver settings (dev/telemetry/scaling_bfs_cg_jacobi.md); a fixed communication cost is a larger share of a 174 ms iteration than of a 526 ms one, but that cannot account for 29 points
- 2026-09-17 D13 P1-M5 measures what MPI and Metis cost a serial user before any extension refactor - why: `HaloExchange` names `MPI.Comm` and `Vector{MPI.Request}` in its fields and every file in the module calls MPI, so the refactor is six steps, and a cost of a fraction of a second would not justify it
- 2026-09-17 D14 the distributed scaling loss is entirely inside the PETSc Krylov solve; XCALibre's own kernels scale superlinearly - why: at 5 mm, `KSPSolve` goes 0.203 to 0.151 s/iter from two to eight ranks (34% efficiency) while everything else goes 0.253 to 0.057 (111%), from `-log_view` (dev/telemetry/scaling_bfs_cg_jacobi.md)
- 2026-09-17 D15 SUPERSEDED BY D16 - proposed per-rank communication-volume imbalance as the proximate cause - why: owned cells are equal to 1.00 at every rank count while ghost cells reach 3.63 max/min at eight ranks, matching the 3.566 message-length imbalance PETSc reports and the 38x wait at `VecNorm`
- 2026-09-17 D16 SUPERSEDES the mechanism in D15: the scaling ceiling is memory bandwidth, not communication imbalance - why: `VecAXPY`, which communicates nothing and is balanced to 1.1, scales at only 63% from two to eight ranks with its work reduced exactly fourfold, and 19 kB against 5 kB of halo cannot produce a millisecond of all-reduce wait
- 2026-09-17 D17 no Metis objective is worth changing for this: volume cuts total communication 11% and leaves the per-rank imbalance at 3.66 against 3.63 - why: the imbalance is the domain's shape, end subdomains having fewer neighbours than interior ones, and it is not the mechanism anyway (D16)
- 2026-09-17 D18 MPI and Metis stay hard dependencies, with the cost recorded rather than removed - why: they cost a serial user 0.17 s of an 0.809 s package load and 27 MB of artifacts, against a six-step refactor that would parameterise `HaloExchange` over MPI's types and move every body in the module to an extension (dev/telemetry/serial_cost_of_mpi_metis.md)
- 2026-09-17 D19 SUPERSEDES D16: the scaling ceiling is not memory bandwidth and the mechanism in D16 is withdrawn - why: `VecAXPY` reaches 104.6 GB/s at two ranks against this machine's 89.6 GB/s theoretical DRAM peak and a measured 110.3 GB/s cache-resident ceiling, so it never reads main memory and already runs at 95% of what the hardware allows; the 63% figure in D16 was never divided against the machine's peak (dev/telemetry/scaling_attribution.md)
- 2026-09-17 D20 the strong-scaling loss to four ranks is CPU frequency throttling, not the distributed module - why: with the clock pinned at 2200 MHz (`no_turbo=1`) efficiency is 102/94/71% at n=2/4/8 on 499,503 cells and 101/93/78% at n=2/4/6 on 1,320,368 cells, against 87/73/47% and 85/68/52% unpinned, where cores fall from 4400 to 3100 MHz as rank count rises
- 2026-09-17 D21 mesh size does not change strong-scaling efficiency, so the loss is a fixed fraction of the work - why: a 2.6x larger mesh gives 101/93% at n=2/4 against 102/94%, refuting both a communication-bound loss, which would improve with volume-to-surface ratio, and a DRAM-bandwidth loss, which would worsen with a 32 MB working set against 12 MB
- 2026-09-17 D22 WITHDRAWS the earlier OpenFOAM efficiency comparison and replaces it - why: it read OF clocks sampled after `foamRun` exited, so idle cores at 2000-2271 MHz, and set XCALibre's Cg+Jacobi against OF's PCG+diagonal rather than OF's production GAMG; at a pinned clock XCALibre ties OF GAMG at one rank, 1.0665 against 1.0620 s/iter, and scales better at every rank count, 102/94/71% against 95/79/62%
- 2026-09-17 D23 the residual loss above four ranks is reduction cost and is deferred, not closed - why: `VecNorm` is 53% of the eight-rank solve against 2% at two ranks with a 38.2x rank imbalance, and excluding it the solve scales at 71% rather than 34%
- 2026-09-17 D24 `test/Project.toml` must declare `Random` - why: `test_halo.jl` uses it and `Pkg.test` resolves exactly the declared dependencies, so the distributed gate failed under the repository's own test command while passing standalone from a richer environment
- 2026-09-18 D25 there is NO regression from six to eight ranks; the degradation is gradual from four ranks on - why: incremental efficiency per added rank is 102/93/87/86% at 1->2/2->4/4->6/6->8 on 499,503 cells and 101/92/84/88% on 1,320,368, so the 6->8 step costs the same as the 4->6 step on both meshes and full core occupancy shows no step change
- 2026-09-18 D26 BoomerAMG does not help this case and is not the distributed default - why: 0.2744 s/iter against Jacobi's 0.1877 at eight ranks, 46% slower, with a scaling curve no flatter (70.0% against 71.0%); 500k cells of laminar BFS is too well conditioned for the setup to amortise, and this was measured WITH hierarchy reuse active
- 2026-09-18 D27 pipelined CG does not recover the high-rank loss - why: 0.2200 s/iter against 0.1877 at eight ranks, 17% slower, with `KSP=pipecg` confirmed in the solver log; the extra per-iteration work needed to enable the reduction overlap exceeds the barrier wait it hides at this scale
- 2026-09-18 D28 `BoomerAMG(reuse=N)` FREEZES the preconditioner, it does not update coefficients - why: `KSPSetReusePreconditioner(PETSC_TRUE)` skips `PCSetUp` entirely, so N-1 solves apply a stale hierarchy and the Nth does a full hypre rebuild; freezing is worth 2x (0.8416 s/iter at reuse=1 against 0.4216 at reuse=10, n=4) and the optimum is near reuse=25 at 0.3951, so the default leaves about 6% unclaimed
- 2026-09-18 D29 GAMG, not BoomerAMG, is the preconditioner whose update semantics match a fixed mesh - why: `reuse_interpolation=true` keeps aggregation and prolongation and recomputes only RAP and the smoothers from the current matrix, which is the coefficient-only update a non-refining mesh permits, whereas PETSc's PCHYPRE exposes only freeze or full rebuild
- 2026-09-18 D30 our AMG is the weak configuration, not the distributed module - why: OpenFOAM's GAMG beats XCALibre's BoomerAMG by 1.24x to 1.45x at every rank count, while XCALibre's Cg+Jacobi beats OpenFOAM's GAMG by 6% to 16% at two ranks and above, so best-config against best-config XCALibre leads and AMG-against-AMG it does not
- 2026-09-18 D31 GAMG has the best scaling curve of every configuration tested and costs the same as BoomerAMG - why: 102.6/97.4/85.6/72.8% at n=2/4/6/8 against BoomerAMG's 96.7/92.6/75.6/70.0 and Jacobi's 101.6/94.5/82.6/71.0, at 0.2749 s/iter against BoomerAMG's 0.2744 at eight ranks; it is still 1.46x slower in absolute terms than Cg+Jacobi on this case
- 2026-09-18 D32 the coefficient-only update is worth 2.69x, more than BoomerAMG's freeze buys, and without the staleness - why: GAMG costs 0.4108 s/iter at four ranks with `reuse_interpolation` on and 1.1060 with it off, against BoomerAMG's 2.0x from freezing (0.8416 at reuse=1 to 0.4216 at reuse=10), so keeping the hierarchy and recomputing RAP beats both rebuilding and freezing
- 2026-09-18 D33 AMG breaks rank invariance and Jacobi does not, which is a documented consequence rather than a defect - why: after 100 SIMPLE iterations the pressure residual spans 2.4x across rank counts under BoomerAMG (7.85e-5 to 1.89e-4) and 0.3% under GAMG, against agreement to 15 significant figures under Jacobi, because an AMG hierarchy is built from the local partition and the partition changes with rank count
- 2026-09-18 D34 OpenFOAM's GAMG beats ours because our SETUP dominates, not our cycles - why: at one rank `PCSetUp` costs 26.39 s against `PCApply`'s 9.23 s, 2.9x, with `MatPtAPNumeric` alone at 12.11 s; PETSc GAMG forms Galerkin coarse operators by sparse triple product every solve while OpenFOAM's agglomeration multigrid sums fine coefficients into agglomerated cells with cached addressing, so their whole solve costs less than our setup, even though we converge in 2-3 iterations against their 6.7
- 2026-09-18 D35 stacking the freeze on the coefficient update closes the gap to OpenFOAM - why: `GAMG(reuse=25)` gives 1.1065/0.3023/0.2115 s/iter at n=1/4/8 against OpenFOAM GAMG's 1.0620/0.3354/0.2127, so we lead by 1.11x at four ranks, tie at eight and trail 4% at one; the freeze is worth 1.30x to 1.42x because it skips the `PCSetUp` that was 31% of runtime
- 2026-09-18 D36 GAMG keeps its rank invariance under the freeze, unlike BoomerAMG - why: the pressure residual spread across rank counts is 0.50% with `reuse=25` against 0.3% unfrozen and 240% for BoomerAMG, so the freeze costs almost nothing in reproducibility here
- 2026-09-18 D37 `Cg()+Jacobi()` remains the fastest configuration on this case and stays the documented default; `GAMG(reuse=25)` is the AMG to recommend - why: Jacobi is 0.1877 s/iter at eight ranks against frozen GAMG's 0.2115, 11% faster, and 500k cells of laminar BFS is too well conditioned for any AMG setup to amortise; AMG's case must be made on a stiffer problem
- 2026-09-18 D38 `reuse` on `BoomerAMG`/`GAMG` is renamed `freeze`, with `reuse` kept as a deprecated alias for one release - why: the count sets `KSPSetReusePreconditioner`, which skips `PCSetUp` for N-1 solves (D28), so the preconditioner is frozen rather than updated; "freeze" also avoids clashing with GAMG's `reuse_interpolation`, which is the coefficient update
- 2026-09-18 D39 `GAMG` freeze default moves 1 -> 25; `BoomerAMG` stays at 10 - why: GAMG final p residual at n=4 after 100 iters is 2.953e-4/2.950e-4/2.954e-4 at freeze 1/10/25 (`dev/telemetry/freeze_residuals.md`) so the pinned 1.36x (D35) is free, while BoomerAMG freeze 25 ends at 1.85e-4 against 7.83e-5 at 10, 2.36x, outside S2's 2x band
- 2026-09-18 D40 the AMG recommendation moves from `BoomerAMG` to `GAMG` and the rank-invariance caveat is documented in both docstrings and the user guide - why: GAMG at its best freeze is faster than BoomerAMG at its best at every rank count (0.3023 vs 0.3951 s/iter at n=4, `dev/telemetry/scaling.csv`) and its residual spread across ranks is 0.50% against 2.4x (D36)
- 2026-09-18 D41 S1 amended: the `reuse` deprecation alias is removed, `freeze` is the only keyword - why: `BoomerAMG`/`GAMG` are unreleased (added under v0.6.1-DEV in CHANGELOG.md, absent from origin/main and every tag), so the alias protects no user and is dead code
- 2026-09-18 D42 the PETSc path no longer substitutes `convergence` for `atol` when `atol`=`rtol`=0; `convergence` is the outer-loop target only, as in serial - why: the outer loop already checks it on the Allreduced residual (`Solvers_1_SIMPLE.jl` convergence check), Krylov runs to itmax at zero tolerances, and PETSc at itmax=1 does not throw (P1-M8 probe), so the substitution was the only divergence.
- 2026-09-18 D43 the `wall_distance!` non-convergence note is withdrawn from docs and roadmap - why: its threshold is the user's `y` `convergence` (`Calculate_4_wall_distance.jl`); the 1e-15 came from the distributed test harness, which sets unreachable targets by design (`test/distributed/psimple_case.jl` header).
- 2026-09-18 D44 PETSc mapping gains `Cgs`->cgs and `DILU`->bjacobi (ILU(0) blocks); unmapped types defer to `petsc_options` and error only if it names none; no other KSP/PC gets a type - why: `DILU` is in 11 example/test setups and errored distributed; the passthrough the error advertised was unreachable (type error fired before options merged); asm/sor/pipelined types lack a measured win (D26, D27) and stay passthrough-only.
- 2026-09-18 D45 single-process PETSc is documented as `distribute` under plain `julia`, not built as a serial-`Mesh` PETSc path - why: 2D BFS 20 iters ran in one process via MPI singleton with Jacobi and GAMG (P1-M8 probe); a serial-mesh path would duplicate `wrap_eqn`/`residual`/`setReference!` dispatch for no measured demand.
- 2026-09-18 D46 the distributed user guide states mechanisms only and its API runs as doctests at one rank (PETSc added to the docs env); measured figures leave the guide - why: user request; case-specific numbers aged within the phase (D16 withdrawn by D19).
- 2026-09-18 D47 GPU fields with a PETSc lacking the device package always error; the `solve_on=CPU()` host-copy opt-in is removed (P1-M9), and a device-resident path without per-solve host staging is built in P1 (P1-M10) - why: user ruling that a GPU run must not be forced onto the host; `passemble!`/`psolve!` stage A, b and x through host vectors on every path today.
- 2026-09-18 D48 P1 defers nothing: every item flagged for later (D23 reductions, D26 AMG retest, freeze-vs-mesh-size) becomes a P1 milestone (P1-M11, P1-M12) - why: user ruling that the release-polish phase carries no deferred work.
- 2026-09-18 D49 distributed LKE/LES, Float32 hypre, multi-GPU scaling validation and AMD parity stay non-requirements of P1 and are not deferred work - why: user ruling that they are real non-goals.
- 2026-09-18 D50 `DILU` keeps its PETSc block-ILU(0) mapping but warns once that it is a substitute (added to P1-M9) - why: user ruling; the mapping is a different preconditioner and must not be silent (D44).
- 2026-09-18 D51 PETSc assembly is COO (`MatSetPreallocationCOO` once, `MatSetValuesCOO` on the nzval pointer per solve) and vectors copy through PETSc.jl's device-aware `withlocalarray!`, with a device-wide sync either side of the solve; host staging buffers and the in-place `MatConvert` are gone - why: GPU 0.097 to 0.073 s/iter on BFS 5 mm n=1, CPU n=2 median 0.457 to 0.445 (noise), residuals unchanged (`dev/telemetry/device_resident_petsc.md`).
- 2026-09-18 D52 distributed PETSc solves on AMD GPUs error at setup rather than run - why: PETSc.jl ships a CUDA memory backend only, so a HIP Vec cannot be handed back as a ROCArray and the device-resident path (D51) cannot run there; AMD parity is a non-goal (D49).
- 2026-09-18 D53 every PETSc KSP takes `rtol` relative to the warm-started initial residual (`KSPConvergedDefaultSetUIRNorm`), as Krylov.jl does; D42's claim that tolerances already matched was wrong for rtol's reference - why: with PETSc's default right-hand-side reference the distributed BFS ended 30 iterations 10-30x above serial (p 1.10e-3 vs 1.09e-4), with it 1.24e-4; PETSc computes the default reference in the running norm (iterativ.c:1541-1572), so this is independent of D54 (`dev/telemetry/tolerance_semantics_and_reductions.md`).
- 2026-09-18 D54 a resolved `cg` KSP uses the natural norm unless the user names one - why: Krylov.jl CG stops on sqrt(r'Mr) (cg.jl:164); drops one reduction per CG iteration (VecNorm 1325 to 120 at n=2); keyed on the merged type so a passthrough -ksp_type never inherits it.
- 2026-09-18 D55 the eight-rank loss is not the reduction COUNT, so D23's framing is restated: cutting ~3.1 to ~2.1 reductions per CG iteration left the n=2 to n=8 speedup at 2.83 against 2.88 - why: equal-thermal runs in `dev/telemetry/tolerance_semantics_and_reductions.md`; the next candidates are single-reduction CG (one screen) and the MatMult/ghost imbalance behind the VecNorm wait (D15).
- 2026-09-18 D56 P1-M12 is superseded by P1-M13: BoomerAMG stays exposed, and the milestone characterises every distributed preconditioner for users and sets defaults from measurement instead of deciding keep-or-drop - why: user ruling that keeping BoomerAMG costs little and what users need is what each solver does, does not, and sensible defaults.
- 2026-09-18 D57 P1-M13 draws preconditioner behaviour from published sources (PETSc/hypre manuals and papers, CFD solver documentation) and runs only the XCALibre measurements needed to confirm or contradict them on this framework - why: user ruling; literature already answers the general questions at no run cost.
- 2026-09-18 D58 single-reduction CG (`-ksp_cg_single_reduction`) is refused - why: equal-thermal n=8 0.268/0.272 s/iter against 0.231/0.235 without it, n=2 unchanged within noise (`dev/telemetry/tolerance_semantics_and_reductions.md`).
- 2026-09-18 D59 the eight-rank wait is inside the PETSc solve, not in XCALibre's work before it: ranks reach each assembly within 0.022-0.153 s total over 23 outer iterations, while -log_sync shows KSPSolve at 1.55 s of balanced work against 4.25 s unsynchronised, with VecScatterEnd at ratio 30 - why: `scaling_probe.jl wait=1` and `-log_sync` at n=8 (`dev/telemetry/tolerance_semantics_and_reductions.md`); ghost cells per rank span 654-2373, so halo exchange is the next suspect.
- 2026-09-18 D60 P1-M11 closes as delivered and the index-width mechanism opens as P1-M14 - why: the stock PETSc_jll Float64 list is Int64 first, so `_petsclib` picks 64-bit indices (16 bytes per nonzero against 12) while MatMult is the memory-bound kernel (scaling_attribution.md); it acts at every rank count, so it is not the reduction-count milestone's mechanism.
- 2026-09-18 D61 PETSc takes the narrowest-index library of the field precision that addresses the global nonzero count, chosen from an all-reduced count so every rank agrees - why: 32-bit indices cut per-iteration time 5% at n=2 and 12% at n=8 with bitwise-identical residuals (`dev/telemetry/petsc_index_width.md`); both stock Float64 libraries carry hypre, so no capability is lost.
- 2026-09-18 D62 P1-M13 maps `IC0GPU` to block-Jacobi ICC(0) and `ILU0GPU` to block-Jacobi ILU(0) with the DILU-style substitution warning, documents ASM, SOR and HPDDM as `petsc_options` recipes, and measures GPU BoomerAMG on the local CUDA-hypre PETSc; AmgX is out (not in the local build) and distributing XCALibre's native AMG is a separate project outside P1 - why: user ruling; the local build has PETSC_HAVE_CUDA, PETSC_HAVE_HYPRE and HYPRE_USING_CUDA, no AmgX.
- 2026-09-18 D63 BoomerAMG defaults gain `P_max=4` (hypre's recommendation with ext+i); declaring pure-Laplacian matrices SPD is withdrawn - why: BoomerAMG 0.652 to 0.639 s/iter with p residual 6.19e-5 to 6.25e-5; the SPD flag left GAMG residuals bitwise identical and time within noise, so it bought nothing measurable (`dev/telemetry/preconditioner_guidance.md`).
- 2026-09-18 D64 `IC0GPU` and `ILU0GPU` run distributed as per-rank block ICC(0) and ILU(0), each warning once per type - why: both completed the 5 mm n=2 case (p 9.94e-5 at 30 iterations against Jacobi's 1.24e-4, `dev/telemetry/preconditioner_guidance.md`); D62 ruling.
- 2026-09-18 D65 defaults stay (GAMG freeze 25, BoomerAMG freeze 10, Jacobi for momentum) and the guide recommends Jacobi for small partitions, GAMG for large pressure meshes and on the GPU, BoomerAMG where GAMG converges poorly; AMG costs roughly a third more memory per rank - why: S2/S5 in `dev/telemetry/preconditioner_guidance.md` agree with the literature: Jacobi x3.73 vs GAMG x3.13 for x2.64 cells, AMG gain gone at ~62k cells per rank, GPU GAMG 0.059 vs Jacobi 0.075 s/iter, peak RSS per rank at 4 mm n=2 Jacobi 2.73, BoomerAMG 3.48, GAMG 3.69 GB.
- 2026-09-18 D66 `petsc_options` also takes a named tuple keyed by `all` and the equation labels, with unknown keys an error - why: a String reaches every solve, so ASM for momentum and GAMG for pressure could not be combined; PETSc option prefixes would have meant renaming every curated option key.
- 2026-09-18 D67 P1 gains P1-M15 (example comment blocks to the three-line limit) and P1-M16 (memory footprint: measured breakdown first, then cures); memory cures touching code outside the distributed work go on one branch off main as a single memory PR, not one PR per cure, while distributed-only cures land on HM/distributed-draft - why: user ruling after the 4 mm BFS at 8 ranks with AMG exhausted this 14 GB machine, with about 4 KB per cell before PETSc.
- 2026-09-18 D68 P1 gains P1-M17 (MPI/PETSc setup guide, including the CUDA route), ordered before P1-M15 and P1-M16 - why: user ruling; users compiling PETSc is the main barrier to GPU runs, and the page tells them only that preferences exist.
- 2026-09-18 D69 The setup guide leads its GPU route with the conda-forge `petsc=*=cuda12_real*` build (no compiling), OpenMPI variant preferred as CUDA-aware, MPICH variant with `-use_gpu_aware_mpi 0`; compiling PETSc becomes the fallback - why: both variants pass `test_gpu.jl` at n=1,2 on the device, deltas 1e-11 vs serial (`dev/telemetry/conda_cuda_petsc.md`)
- 2026-09-18 D70 The guide tells GPU users of the conda build to use `GAMG()` or `Jacobi()` for pressure and states that `BoomerAMG()` crashes there; turning that crash into an error is left for a separate milestone, not P1-M17 - why: BoomerAMG with device fields segfaults at n=1,2 on host-only hypre while GAMG passes (`dev/telemetry/conda_cuda_petsc.md`); PETSc exposes no runtime query for hypre's device support, so the fix needs its own design
- 2026-09-18 D71 Distributed GPU solves pick the communication path from `MPI.has_cuda()`: direct when CUDA-aware (one `@info`), otherwise PETSc's exported `use_gpu_aware_mpi` flag is cleared so messages stage through the host (one `@warn`); an explicit `-use_gpu_aware_mpi` in `petsc_options` wins - why: user ruling; R2 forbids needing an env var, PETSc aborted at first device use on both conda MPIs without one, and a start-up option is lost when PETSc was initialised earlier (`dev/telemetry/conda_cuda_petsc.md`)
- 2026-09-18 D72 `AUDIT.md` reshapes the rest of P1: milestones P1-M18 to P1-M24 (release blockers, gate widening, O(N+P) extraction, owned-row single-copy system, fused communication, parallel preprocessing, binary I/O with restart) and the outcome gains "structurally ready to scale"; spec gains R11 and R12 - why: the audit found the module correct within 8 ranks and one GPU but capped by rank-0 O(P·N) preprocessing, 3.5-4x matrix multiplicity at 4.1 KB/cell peak, and 8 blocking exchange rounds plus 12 device drains per SIMPLE iteration (`AUDIT.md`).
- 2026-09-18 D73 multi-node, multi-GPU and AMD validation is phase P2 on the HPC, after M18-M24 land; P1 takes what is correct and performant on this machine (n=1-8 CPU, n=1-2 on one GPU) as sufficient - why: user ruling; no such hardware here, and every M18-M24 verdict is measurable locally.
- 2026-09-18 D74 the owned-row CSR enters through one mesh seam (`n_rows(mesh)` = `length(mesh.cells)` serial, `n_owned` distributed) so shared kernels change only where their row range comes from; the shared-code memory cures (sparsity shared across equations, preallocated connectivity, no per-equation host mesh copy) stay on D67's single PR off `main` - why: keeps D67 while letting the distributed cure land on this branch with serial code paths unchanged.
- 2026-09-18 D75 M18-M24 are implemented by Fable (claude-fable-5-1) in ONE fresh session, in `dev/phaseRoadmap.md` order, landing every step and not stopping at milestone boundaries - why: user ruling; the plans carry mechanism, cost and verdict per step, which is what a one-shot session needs.
- 2026-09-18 D76 P1-M16 is restated as the memory breakdown only (its S1); its cures are P1-M21's steps and the D67 PR - why: the audit split memory into four sources (rank-0 mesh, matrix multiplicity, setup transients, GC per rank), each with its own cure and owner (`AUDIT.md` § Memory).
- 2026-09-18 D77 `AUDIT.md` moves to `dev/archive/reviews/p1/audit-2026-09-18.md` when P1-M18 closes, and the plans cite that path - why: the audit itself lists stray root-level documents as a release blocker.
- 2026-09-19 D78 partition files start with a plain-text header (format, julia, xcalibre, nranks, TI, TF) checked before anything is deserialised, with exact version match required and `distribute(reader; dir)` regenerating on mismatch - why: `.jls` parts failed after upgrades deep inside the solver (audit blocker 6); a text header needs no serializer compatibility to be read.
- 2026-09-19 D79 the BoomerAMG GPU guard asks hypre for its execution policy (`HYPRE_GetExecutionPolicy` == device), not its memory location, and `BoomerAMG(device=true)` skips it - why: measured 2026-09-19 on the conda host-only and the custom CUDA hypre: memory location reports device (1) on BOTH builds because a CPU hypre maps device memory to host, execution policy reports 0 vs 1.
- 2026-09-19 D80 `initialise_writer(VTK, DistributedMesh)` returns a sentinel writer whose `write_results` errors, rather than erroring at setup - why: the writer is built before `write_interval` is known and VTK is the default output, so a setup error would break every 2D distributed run that writes nothing; `save_output` dispatches on the physics type, so the hook is `write_results`.
- 2026-09-19 D81 `DistributedMesh` stores its communicator and every exchange, reduction, solver and gather defaults to it; a part received over MPI or loaded from disk is re-stamped with the caller's comm - why: `MPI.Comm` serialises as a raw handle (an Open MPI pointer) that means nothing on another rank, checked 2026-09-19.
- 2026-09-19 D82 `bind_device!(backend)` binds by node-local rank from `Comm_split_type(COMM_TYPE_SHARED)` and warns once when a node's ranks exceed its devices; `bind_device!(backend, rank)` stays for explicit control - why: global-rank binding is wrong whenever ranks do not fill nodes contiguously with one rank per GPU (audit blocker 4).
- 2026-09-19 D83 the hypre execution-policy query initialises hypre first when `HYPRE_Initialized` is false - why: querying an uninitialised hypre 3.1 creates its handle and PETSc's later `HYPRE_BoomerAMGCreate` then fails with error code 1 (stock env, `test_hypre.jl` n=1, 2026-09-19); PETSc itself calls `HYPRE_Initialize` before creating the preconditioner.
- 2026-09-19 D84 P1-M18-S2 to S10 land as one commit under one CPU verdict (suite 12/12 at n=1,2), with the GPU verdicts of S2, S6 and S10 taken by the S11 runs - why: nine small edits sharing files, and the GPU test could not run to its assertions until the S11 segfault was reproduced and understood.
- 2026-09-19 D85 the PETSc extension snapshots the eleven signal dispositions PETSc resets and restores them after building a solver, instead of a launch flag or thread setting - why: PETSc's CUDA device init pops an empty signal-handler stack and leaves SIGSEGV at SIG_DFL, so Julia's safepoint faults on the interactive thread killed 3 of 15 GPU runs silently; restored, 26/26 clean and `test_gpu.jl` green at n=1,2 on both CUDA envs (`dev/telemetry/conda_cuda_petsc.md`); R2 forbids the `-use_gpu_aware_mpi 0` or `-t 1,0` workarounds.
- 2026-09-19 D86 `extract_subdomain` reads from a `_PartIndex` built once per decomposition (cells, interior faces and boundary faces bucketed per part in one pass; reset-on-exit `Vector{TI}` inverse maps; the dual graph taken from `faces_range`/`cell_neighbours` with rows sorted and deduplicated) - why: parts bitwise identical on all three configurations; `decompose` 1.77 to 0.97 s at 5 mm P=64, 0.82 to 0.58 s at P=8 (`dev/telemetry/extraction_cost.md`); the 5x the plan asked for was not there to take, since the P·N term was 1 s of 1.77 at this size.
- 2026-09-19 D87 the communication budget is asserted per SIMPLE iteration as 7 exchanges and 6 all-reduces on 2D meshes (8 and 8 in 3D) from global counters in the halo exchange and the solver seams; `runtests_mpi.jl` defaults to ranks 1,2,3,5 and the gate runs at 2 and 3 - why: the 2D BFS has two momentum components, so the audit's 8/8 is the 3D figure; gate 10/10 in 196 s against Q2's 300 s.
- 2026-09-19 D88 the distributed driver wraps its (file, rank) loop in one outer testset - why: a top-level for-testset throws at the end of the first failing iteration, so one failing file silently skipped every later file and rank count (seen twice on 2026-09-19).
- 2026-09-19 D89 `test_invariance.jl` holds residual histories across rank counts to a relative 1e-4 per iteration (Q1), not the plan's 1e-12 - why: n=2,3,5 against n=1 measured 2.0e-6, 2.7e-6, 2.9e-6 max relative spread over 50 iterations (BFS, Jacobi, `rtol=1e-8`); PETSc's parallel dot products change the reduction order, so bitwise histories were never on offer.
- 2026-09-19 D90 P1-M16 delivered: the per-rank memory baseline is 1950 MB peak at 4 mm n=2 (about 790 MB fixed + 1.85 KB per local cell), replacing 2.79 GB, which was three `run!` setups in one process; the fixed runtime and compiled code are the largest item below ~440k cells per rank and no planned cure touches them - why: `dev/telemetry/memory_breakdown.md`; same parts through `scaling_probe.jl` peak at 2838 MB.
- 2026-09-19 D91 P1-M21-S2 is absorbed by P1-M16 and P1-M21-S3 (no COO map on host matrices) goes ahead - why: PETSc's two 8-byte-per-nonzero COO arrays are 99.2 of its 290 MB per rank at 4 mm n=2, 34 percent against the 15 percent bar (`dev/telemetry/memory_breakdown/petsc_sites_4mm_rank0.tab`).
- 2026-09-19 D92 P1-M21-S1 (owned-row CSR through `n_rows`) is DEFERRED TO P1-M22 as a kernel-range step, its rectangular-CSR half only if M22's timing shows ghost rows matter; D74's ordering of S1 first is amended, since S3 and S4 need only the owned CSR prefix, which exists today - why: ghost rows are 0.46/2.94/10.77 percent of local rows at n=2/8/64 on the 5 mm BFS (`dev/telemetry/memory_breakdown.md`), about 1 MB of 205 MB equation storage at the M21 measurement point.
- 2026-09-19 D93 P1-M21-S3 fills PETSc's diagonal and off-diagonal SeqAIJ blocks directly from the local CSR after one `MatCreateMPIAIJWithArrays`, not through `MatUpdateMPIAIJWithArray` - why: PETSc 3.24 `mpiaij.c:4305` requires values in global-sorted column order per row, while the local CSR lists owned columns then ghosts; ghosts are sorted by (owning rank, id) (`Distribute_1_partition.jl:202`), so each segment already matches its block and no map is needed.
- 2026-09-19 D94 P1-M21-S3 landed: host PETSc matrices carry no COO map - why: PETSc heap 290 → 206 MB and natural peak 1950 → 1793 MB per rank at 4 mm n=2; 20-iteration residual hashes bitwise equal at n=2 and n=8; gate 10/10 and test_periodic/perf/invariance/ghosts green at n=2,3 (`dev/telemetry/memory_breakdown.md`).
- 2026-09-19 D95 P1-M21-S4 landed: PETSc x and b own no storage and borrow the field's owned prefix and the equation's b per solve (`Vec(CUDA)PlaceArray`); `test_perf.jl` warms each measured call once before measuring - why: steady-state allocations at n=2 psolve! 3136 → 1664 B and passemble! 6448 → 4880 B, residual hashes bitwise equal at n=2 and n=8, gate 10/10, `test_gpu.jl` n=1,2 green; the test had been measuring a first call's thunk compilation (psolve! 8416 B first call, 1664 B after).
- 2026-09-19 D96 the per-rank heap recipe is `--heap-size-hint` of about node memory over ranks per node, after a plain-session precompile, documented in the distributed guide - why: 4 mm BFS n=2 at 1200M peaks 1667/1663 MB against 1772/1759 MB without it, residual hash unchanged; ranks loaded existing caches under the flag, so the earlier cache-flag failure applies only to a rank that must precompile.
- 2026-09-19 D97 REFUSED: `GC.gc(true)` before each distributed solver is built - why: 4 mm BFS n=2 peak 1773/1759 MB with it against 1772/1747 MB without; the high-water mark is set during the iterations, not at setup, and freed Julia memory is not returned to the OS.
- 2026-09-19 D98 each process keeps one live PETSc solver per equation label and destroys it when the label is wrapped again, so repeated `run!` calls no longer leak - why: PETSc heap after three extra `run!` calls at 5 mm n=2 went 70/140/210/280 MB before, 70/70/70/70 MB after; labels are unique within a run and every rank wraps in the same order, so the collective destroys match; a finalizer would run at different times on different ranks.
- 2026-09-19 D99 P1-M21 closes as delivered with S1 deferred to P1-M22-S8 (D92) and the time band measured at n=2 and n=4 instead of n=8 - why: peak per rank 1950 → 1772 MB at 4 mm n=2 and 1202 → 1152 MB at 5 mm, per-iteration time inside the 3 percent band at n=2 and n=4 and on the GPU, CPU residuals bitwise equal; n=8 at 5 mm needs about 7 GB beside the 2.8 GB language server and the memory guard stopped it.
- 2026-09-19 D100 P1-M25 (per-rank fixed memory) is opened and ordered before P1-M22 - why: user ruling; the fixed cost is about 790 MB per rank, 41 percent of the 4 mm n=2 peak and more than the whole per-cell cost below about 440k cells per rank (`dev/telemetry/memory_breakdown.md`), and no other milestone touches it.
- 2026-09-19 D101 every gate and experiment run whose result decides a verdict finishes within five minutes of wall clock; a verification set that would exceed it is cut to what the change can reach, or the case is made smaller or faster, never run longer - why: user ruling; step verifications of 20+ minutes (suite batches, a four-version timing sweep) made three trials cost an hour.
- 2026-09-19 D102 P1-M25-S1 delivered: fixed per-rank cost splits into about 250 MB shared per node and 324 MB private per rank after load (n=4, 10 mm); first-run growth is GC heap and malloc, JIT code only 2 MB; removable package rows are PETSc.jl image 47 private and Pkg 17 - why: `dev/telemetry/fixed_rank_memory.md`, runs 16 s and 20 s wall
- 2026-09-19 D103 P1-M25-S2 delivered: removable private MB per rank ranks first-run compilation (about 190 of 229 growth: GC live +27 only, second run! +21, malloc_trim returns 28) then PETSc.jl +102 over XCALibre+MPI (ten libpetsc variants dlopened, 87 MB image); Pkg arrives via MPI and LLVMExtra_jll so is not removable here; GPUArrays/LLVM 70-95 is a direct dependency, out of scope - why: `dev/telemetry/fixed_rank_memory.md` § S2
- 2026-09-19 D104 the first-run private growth is GC page retention, not compiler memory, and the M25 exit baseline is private at `iterations` rank 0 10 mm n=4 gc=0 = 556 MB (hash dbc3c69ab48b3394); the PETSc.jl row is 47 MB private per rank at n=4, not 102 - why: GC bytes_resident 254 vs 51 live, `--heap-size-hint=250M` cuts private 556 → 390 with hash unchanged; telemetry § S2
- 2026-09-19 D105 XCALibre will not set a per-rank GC memory target at runtime (user option 1 refused on data); the heap-size hint stays a user launch flag documented in S5 - why: steady-state private 459 vs 472/500 baseline, +11-17 percent `run!` time, telemetry § S3
- 2026-09-19 D106 runtime compilation causes the first-run private growth: harvested precompile statements loaded before setup cut private at `iterations` 559 → 359 MB (−36 percent) and `t_iter_s` 11.46 → 0.04 at 10 mm n=4 with hash unchanged; 256 of 937 statements bake a rank-local kernel size, so harvested statements cannot ship as-is - why: telemetry § S4, `dev/telemetry/memory_breakdown/m25s4_*.txt`
- 2026-09-19 D107 precompiling only case-independent signatures gains nothing (private 572 vs 559, `t_iter_s` 11.15 vs 11.46), so a shippable precompile must cover the case-typed solver call tree and size-free kernels - why: `dev/telemetry/memory_breakdown/m25s4_generic.txt`
- 2026-09-19 D108 user chose option 3: the per-case precompile recipe is documented now (P1-M25-S4) and shipped precompilation with size-free kernel launches opens as P1-M26, placed after P1-M24; the recipe run literally cuts private 556 → 359 MB and compile 12.1 → 0.04 s at 10 mm n=4 once `preserve=Pkg.PRESERVE_ALL` and env-copied deps replace `Pkg.add` - why: telemetry § S4, `dev/telemetry/memory_breakdown/m25s4_recipe*.txt`
- 2026-09-19 D109 P1-M25 closes as delivered: exit met by the documented per-case recipe (10 mm n=4 private −35 percent, bar 30) with the floor attributed; the saving reaching users without a recipe is P1-M26; 5 mm n=2 peak 1160 → 1009 MB, hash unchanged; src untouched so the gate is not re-run - why: telemetry § S4 and § Close
- 2026-09-19 D110 P1-M22-S1 lands as one shared width-keyed halo schedule per mesh, tags 10·width (adjoint +1), `DistributedEqn` without its own halo; `HaloCache` keeps its untyped slots instead of becoming parametric, because `write_parts` serialises the mesh and any layout change invalidates every cached part while `halo_exchange!` is already the function barrier — why: halo alloc 4016→2928 B (n=2), hashes bitwise unchanged at 10 mm n=2,4, gate 10/10, `test_gpu` n=1,2 green; `dev/telemetry/communication.md`
- 2026-09-19 D111 P1-M22-S2 lands by snapshotting only the owned diagonal of the x (and in 3D y) system after its solve, then one width-3 exchange and the residuals against the saved diagonals; neither the plan's full (nzval, b) snapshot nor keeping width-1 exchanges for x and y is needed, because `update_equation!` restores A0 and boundary conditions and relaxation touch only the diagonal and that component's b — why: hashes bitwise unchanged at 10 mm n=2,4 (3D), exchanges 8 → 6 (3D), `check_ghosts` U zero; `dev/telemetry/communication.md`
- 2026-09-19 D112 P1-M22-S3 lands: residuals are local (num, den) sums reduced by one in-place `Allreduce!` per equation (6 values for U, 2 for p) through a host buffer on `DistributedEqn` — why: all-reduces 8 → 2 per 3D iteration with hashes bitwise unchanged at 10 mm n=2,4; `dev/telemetry/communication.md`
- 2026-09-19 D113 P1-M22-S4 lands: `sync!` takes a (scalar, vector) tuple packed width 4; `inverse_diagonal!` and `H!` keep their self-sync by default with a `halo=false` opt-out used only by the SIMPLE body, so bodies not yet paired (PISO, CSIMPLE, MRF, film, multiphase) stay correct untouched; `HaloCache` gains a width-4 slot and the part format goes to 2 — why: exchanges 6 → 5 with hashes bitwise unchanged at 10 mm n=2,4 on regenerated parts, `check_ghosts` rD/Hv zero; `dev/telemetry/communication.md`
- 2026-09-19 D114 P1-M22-S5 lands: each schedule binds persistent send and receive requests to its MPI-facing buffers at build and every exchange is Startall/Waitall; the adjoint takes fresh requests; the plan's pre-post of the next receive after unpack is not done, since a receive left pending at finalize risks a hang and the whole exchange costs 17 µs at n=8 — why: 16.57 vs 16.89 µs per width-3 exchange at 10 mm n=8, alloc −192 B, hashes unchanged; `dev/telemetry/communication.md`
- 2026-09-19 D115 P1-M22-S6 WITHDRAWN: overlapped `sync_begin!`/`sync_end!` is not built and no two-phase API is kept, because the two exchanges it would hide are 0.14 percent of a 10 mm n=4 iteration (about 0.3 at n=8) against a 3 percent bar, and a two-phase split alone added 256 B per exchange; P2 re-measures on the HPC where latency is larger (D73) — why: 28 µs of 20.1 ms; `dev/telemetry/communication.md`
- 2026-09-19 D116 P1-M22-S7 WITHDRAWN: no event handoff to PETSc and the post-unpack halo drain stays, because putting XCALibre on PETSc's own stream with every drain removed, the ceiling of what events can deliver, moves 5 mm n=1 GPU by at most 0.3 percent against a 3 percent bar; PETSc uses the legacy NULL stream through one global context, which answers the plan's open question — why: 57.3/57.2 vs 57.4 ms per iteration; `dev/telemetry/communication.md`
- 2026-09-19 D117 P1-M22-S8 WITHDRAWN: cell kernels keep their full local range, because the five kernels are 12.7 percent of a 10 mm n=4 iteration and ghosts 3.5 percent of rows, a ceiling of 0.44 percent (about 1.4 at n=64), while the change threads a range seam through shared serial kernels — why: profile shares and ghost fraction; `dev/telemetry/communication.md`
- 2026-09-19 D118 P1-M22 closes as delivered with S6, S7 and S8 withdrawn on measured bounds: 5 exchanges and 2 all-reduces per laminar SIMPLE iteration with residual hashes unchanged; the n=8 pinned time is replaced by bounds because 10 mm n=8 with PETSc does not fit beside the language server and every step it would judge is under 0.5 percent; persistent requests are freed at MPI finalize — why: counters in `test/distributed/test_perf.jl`, `dev/telemetry/communication.md`
- 2026-09-19 D119 the M23 binary format is one layout for serial and partitioned meshes: header with an explicit kind (serial or partitioned) plus rank count, a mesh block, and a partition block only when partitioned; a public header query tells users which they hold and loaders refuse the wrong kind with the call to use; the serial writer/reader and serial checkpoints are a separate feature off main after P1 (user) — why: a serial binary mesh and restart are wanted for serial and HPC runs, and fixing the block boundary now avoids a second format migration
- 2026-09-19 D120 amends D119 (user): the header has identical entries for serial and partitioned files (serial: nranks 1, rank 0, n_ghost 0, empty partition block) so one struct and one code path read and write both; the serial loader and writer may be implemented on this branch when appropriate but ship in a separate PR off main together with the serial mesh format — why: one header structure to maintain; serial support is a separate feature
- 2026-09-19 D121 amends D75 (user): M23 onward is implemented by Opus 5 (claude-opus-5) in the current session, same rules (roadmap order, every step committed and pushed) — why: user ruling at resume, 2026-09-19
- 2026-09-19 D122 the `.xdm` part format checks only its own format version (exact) plus kind, rank count and endianness; the XCALibre and Julia versions are recorded but not checked, so parts survive package upgrades; header and blocks are raw little-endian dumps of the isbits mesh arrays with Boundary names written as strings — why: D78's exact-version check existed because `serialize` breaks on any struct change; a layout owned by a format number does not, and regenerating after every upgrade is the cost M23-S1 removes
- 2026-09-19 D123 P1-M23-S1 landed: `partition_mesh` writes binary `rank_<r>.xdm` (format 3), `distribute(dir)` reads it, `mesh_info` is public, `.jls` is gone; serial-kind reader/writer internal only (D120) — why: `test_offline.jl` n=1,2,3 exact on every array incl. 3D and serial kind; gate 10/10 in 193 s; 4 mm n=8 load 0.324 s vs 0.345 s `.jls` (`dev/telemetry/preprocessing.md`)
- 2026-09-19 D124 P1-M23-S2 landed: `distribute(FOAMCase(dir))` reads `processor<rank>/constant/polyMesh` per rank, recognises processor patches by the `procBoundary<a>to<b>` name decomposePar and the XCALibre writer both use, and builds ghosts from one per-face exchange of (centre, volume, global id, original id); original ids from `cellProcAddressing`; OpenFOAM 12 and v2512 place it identically (open question settled) — why: BFS 10 mm scotch n=2,4,8 (OF12) and n=4 (v2512) match serial to 2.2e-9 relative, `check_ghosts` 0, `reconstructPar` completes (`dev/telemetry/preprocessing.md`)
- 2026-09-19 D125 OpenFOAM field files gain a dimensionless `dimensions` entry (serial and decomposed writers), the decomposed writer writes points at 17 significant digits and `cellProcAddressing` — why: `reconstructPar` refuses a field without `dimensions`; 6-digit points made a written case unreadable as the same mesh; the addressing is what lets reconstructPar and `FOAMCase` recover the undecomposed order. Known: writing into a case read with `scale` rewrites its processor meshes in metres
- 2026-09-19 D126 P1-M23-S3 landed: processor-patch rules in `dev/architecture.md`; writer and reader are inverse (partition, global ids, patches, original ids equal; centres to 1e-12, volumes to 1e-10 relative, since geometry is recomputed from points) — why: `test_io.jl` n=1,2,3 green including a solve on the re-read case matching serial below 1e-6
- 2026-09-19 D127 P1-M23-S4 landed: `repartition(dm; method)` partitions the owned-row cell graph with PETSc `MatPartitioning` (direct calls: PETSc.jl's Apply wrapper drops the output IS) and migrates cells with one Alltoallv of verbatim geometry plus an old-to-new id query; default `:ptscotch` — why: 10 mm BFS simple n=8 -> PT-Scotch cut 1970 vs serial Metis 2046, cells max/min 1.019; ParMETIS cut 2089 but max/min 1.097 (its tolerance is max/avg 1.05), failing the 1.05 bar; both match serial to 2.2e-9 (`dev/telemetry/preprocessing.md`). One mesh and one rank count; ParMETIS may balance better elsewhere
- 2026-09-19 D128 migration dedups a face whose two cells arrive on one rank by taking it from the lower old-id cell's record, never by value — why: `FOAMCase` parts orient each interface copy out of their own cell with last-bit-different centres, so value dedup kept both copies and the 8-rank run was off by 0.8 with consistent ghosts
- 2026-09-19 D129 P1-M23 closes as delivered: S1-S5 landed; S5 adds the decomposed-case, repartition and route-choice sections to the guide with a one-rank `FOAMCase` doctest; CHANGELOG (source of the generated release notes) carries the entries — why: docs build exit 0 in 135 s with doctests; gate 10/10; exit criterion met (S2 at n=2,4,8, S4 in the conda env)
- 2026-09-19 D130 P1-M24-S1 landed: the decomposed writer writes OpenFOAM binary (label 32, scalar 64) for mesh and fields and `phi` from a flux the solver hands over through `attach_flux!` (a no-op for other writers); the FOAM reader learns binary; the plan's claim that the reader already parsed binary was wrong — why: write 0.22 s vs 2.0 s ASCII at 4 mm n=2; OpenFOAM 12 checkMesh, foamToVTK, reconstructPar read it and reconstruct serial fields to 1.1e-9 (`dev/telemetry/io.md`)
- 2026-09-19 D131 P1-M24-S2 landed: `run!(...; restart)` resumes distributed incompressible SIMPLE and PISO runs; two hooks in the shared solver bodies restore cell fields, time and dt before the initial calculations and `mdotf` from `phi` after them, so the initial flux, pressure gradient and viscosity are recomputed from restored fields exactly as the loop left them; the writer takes flux and dt through `attach_state!` and writes OpenFOAM's `uniform/time`; k-omega SST rebuilds `nutf` from `nut` through `restart_turbulence!`; wall distance y is read (it is written) — why: resumed SIMPLE, PISO and SST retrace the straight run bitwise at n=2 and within 1e-10 at n=1,2,4 (`dev/telemetry/io.md`); serial runs error on `restart`
- 2026-09-19 D132 P1-M24 closes as delivered: S3 adds "Output, checkpoints and restart" to the guide (binary layout, `write_interval` as checkpoint cadence, `restart` semantics and scope) and a CHANGELOG entry — why: docs build exit 0 in 139 s; exit criterion met (restart green for SIMPLE, PISO, SST at n=1,2,4; write time in `dev/telemetry/io.md`)
- 2026-09-19 D133 P1-M26-S1 landed: every kernel constructed from `_setup(...)...` now goes through `_sized(f!, backend, workgroup, n)`, which keeps an integer workgroup static and passes the range at launch (both at launch under `AutoTune`); the `AutoTune` path keeps its range-dependent workgroup semantics and pays +33 MB private per rank of first-run compilation garbage — why: second mesh size compiles in 0.01 s instead of 3.28 s, hashes bitwise at n=2,4, CPU and GPU time unchanged, a fully dynamic workgroup cost 1.6 percent CPU time so integer workgroups stay static (`dev/telemetry/precompile.md`)
- 2026-09-19 D134 P1-M26-S2 and S3 WITHDRAWN (user): no precompile workload ships; the per-case recipe stays the way to remove first-run compilation, and M26 closes on S1 plus the guide update — why: a workload removes runtime compilation only for exactly matching case types (D107), no shipped mesh reproduces the 3D BFS patch layout, and every install would pay its compile time
- 2026-09-19 D135 P1-M26 closes as delivered on S1 and S4 (S2, S3 withdrawn by D134): the guide says a per-case precompile package stays valid across mesh resolution and rank count and must be retraced only when models, boundary conditions, schemes, solvers or number types change; CHANGELOG records size-free kernels — why: a package traced at 10 mm n=2 cut compile 10.2 → 0.44 s at n=4 and 11.0 → 1.6 s (with five iterations) on 5 mm n=4, hashes unchanged; docs build exit 0 in 135 s
- 2026-09-19 D136 P1-M15 closes as delivered for `examples/` and for every block this branch introduced; 108 long comment blocks that already exist on main (65 files in `src/` and `test/`, e.g. Discretise, ModelPhysics, Mesh) are left for a cleanup PR off main rather than added to the distributed PR, so the vault check stays INVALID until then — why: the check's 12-line cap hid them until `examples/` was clean; a per-file count against `git show main:<file>` finds only 4 introduced here (fixed); code lines unchanged (diff grep empty), every example parses
- 2026-09-19 D137 amends D136 (user chose a PR off main): the long comment blocks are split by file so the two branches never edit the same file; the 20 `src/`/`test/` files this branch already changes are trimmed here, the 45 it does not touch go in `HM/comment-blocks` off main, and the examples were trimmed here in M15 — why: identical comment edits on both branches would conflict at merge; code lines unchanged (diff grep empty), package loads
- 2026-09-20 D138 P1-M27 opened by the user: the motorBike tutorial benchmarks must run distributed, which needs the wall-function empty-patch fix, KOmegaSST and `potential_flow!` - why: 353k-cell motorBike raises `BoundsError` on a 0-element `IDs_range` in `set_production!` at n=6 and n=8 and runs at n=1,2.
- 2026-09-20 D139 P1-M27-S1 guards all six per-patch wall-function launches, not the four reported: `average_wall_cells!` and `fix_wall_row!` index `BC.IDs_range[1]` the same way - why: `grep IDs_range\[1\]` returns six sites in `RANS_functions.jl` (89, 135, 210, 252, 329, 367).
- 2026-09-20 D140 the guard is an early return per patch, not a safe `start_ID` with `ndrange=0` - why: `_setup(CPU, AutoTune, 0)` yields `workgroupsize = cld(0, nthreads()) = 0`, and the six wrappers sequence local kernel launches only, so skipping one skips nothing collective.
- 2026-09-20 D141 P1-M27-S3 is not an audit of `setReference!` but a wiring change: `potential_flow!` never called `wrap_eqn`, so on a `DistributedMesh` every rank built a serial Krylov workspace and solved its own block - why: `grep wrap_eqn src/Solvers` lists SIMPLE, PISO and LAPLACE and not `Solvers_6_potential_flow.jl`, while `Solve.setReference!(::DistributedEqn, ...)` already maps a global cell id.
- 2026-09-20 D142 the serial OpenFOAM writer's `IDs_range[1]` is NOT a site of the empty-patch class - why: the decomposed writer is `Distribute_7_io.jl`, which already uses `first(b.IDs_range)`, and the serial writer never sees an empty patch.
- 2026-09-20 D143 `motorBike_mpi.jl` reports `env_distributed/XCALIBRE_SHA`, a stamp `env_setup.jl` writes, not the loaded code - why: a run on the S1 commit still printed `c8065516`; the loaded code was confirmed instead by `isdefined(XCALibre.ModelPhysics, :no_wall_faces)`.
- 2026-09-20 D144 motorBike at n=8 must not be run unguarded on this machine: it drove MemAvailable to zero and OOM-killed the user's editor - why: 8 ranks x 44k cells still pays the per-rank fixed cost (D102-D109) on a 14 GB box; n=6 reproduces the same empty patches and is the verdict run, under `memguard.sh`.
- 2026-09-20 D145 motorBike at n=6 is also not a verdict run on this machine: `memguard.sh` killed it at MemAvailable 1852 MB during JIT compilation, before the first iteration - why: the kill trace is all LLVM frames and the run had only reached `Metis partition: cells max/min = 59257/58879`; the cure is the per-case precompile recipe (D135), not a smaller rank count.
- 2026-09-20 D146 P1-M27-S2 tests KOmegaSST distributed on the 10 mm backward-facing step with wall functions (`examples/2D_BFS_highRe_kwSST.jl`), not on motorBike (user) - why: the existing `sst_case.jl` uses Dirichlet walls and so covers no wall function, and the small mesh runs at several rank counts inside the five-minute cap.
- 2026-09-20 D147 the distributed `potential_flow!` mismatch is `reconstruct!`, not the solve or the flux: it selects the 2D branch with `typeof(mesh) <: Mesh2`, which a `DistributedMesh` never is, so a distributed 2D case inverted a 3x3 moment matrix that is singular in 2D - why: at n=2 on the 10 mm step Phi matched serial to 1.1e-15 and the corrected face flux to 1.7e-16 while U was off by 0.70, on a cell touching no processor cut.
- 2026-09-20 D148 `setReference!` in the `potential_flow!` corrector loop took the unwrapped equation, pinning local cell 1 on every rank; it now takes the wrapper - why: the distributed method maps a global id and the serial one does not, and the loop was the one call site left unwrapped after D141. Latent on the BFS, where a fixed-pressure outlet leaves the reference `nothing`.
- 2026-09-20 D149 `typeof(mesh) <: Mesh3` is also wrong for a distributed mesh at `Solvers_1_CSIMPLE.jl:347` and `Solvers_1_SIMPLE-MRF.jl:234`, where it skips the z-velocity convergence check - why: same `_base_mesh` omission as D147; left for P1-M27-S4 rather than widened into S3. `Solve_1_api.jl:268` is not a site: the distributed vector solve has its own method.
- 2026-09-20 D150 P1-M27-S1 is proved distributed on the 10 mm step, not on motorBike: `test_turbulence_sst_wallfn.jl` and `test_potential_flow_mpi.jl` are green at n=2 and n=6, 4/4 in 3m01s - why: at n=6 rank 2 holds `top` with no faces, which is the configuration that raised the `BoundsError`, and the pair fits the five-minute cap where motorBike does not (D144, D145).
- 2026-09-20 D151 the distributed gate stays at n=2,3 and does not gate the empty-patch class - why: no patch of the gate's meshes empties below six ranks, so the class is gated by `unit_test_wall_function_empty_patch.jl` in the serial suite and proved distributed by `runtests_mpi.jl --ranks=6,8`.
- 2026-09-20 D152 `potential_flow!` is verified in 3D and with periodic patches, not only 2D: `test_potential_flow_3d_mpi.jl` on the 4 mm periodic cascade is green at n=2 and n=4 in 52 s - why: D147 changes which branch a 2D distributed mesh takes and leaves 3D alone, and the cascade is the only suite mesh whose `pref` is live, which is what exercises D148.
- 2026-09-20 D153 P1-M27-S4 review sweep over the four classes D147 and D148 belong to found no further live defect - why: mesh-type tests resolve to `Solve_1_api.jl:268` (serial vector method, unreachable distributed) and `Solvers_4_Godunov.jl:1347` (density-based); eager `IDs_range[1]` to LES only (a documented gap); local reductions to `Solvers_4_Godunov.jl:1393` only; and every `length(mesh.cells)` is an allocation size, correct as a local count.
- 2026-09-20 D154 the sweep DID find that nothing rejects an unsupported solver on a `DistributedMesh`: CSIMPLE, CPISO, SIMPLE-MRF, Godunov, FilmModel and the LES models never call `wrap_eqn`, so each rank silently solves its own block instead of erroring - why: `grep is_distributed_mesh src/Solvers src/ModelPhysics` lists only LAPLACE, SIMPLE, PISO, potential_flow, KOmega and KOmegaSST, and `Distribute_5_solvers.jl` raises no error for the rest. Same silent-wrong-answer shape as D141. Opened as P1-M27-S5.
- 2026-09-20 D155 `_base_mesh` at `Solvers_1_CSIMPLE.jl:347` and `Solvers_1_SIMPLE-MRF.jl:234` (D149) is correctness-preserving, not a live fix, and is kept so the seam is right when those solvers are wired - why: neither is reachable with a distributed mesh today (D154), so the wrong branch it would have taken is unreachable.
- 2026-09-20 D156 P1-M27-S5 refuses an unsupported combination on a distributed mesh at the solver entry point, through a default-FALSE `distributed_ready` trait plus a `DISTRIBUTED_SOLVERS` list, called by all nine solver entries - why: default-false makes every new solver or model refused until a method declares it wired, so the trait is the implementation inventory the user asked for rather than a list that rots.
- 2026-09-20 D157 the distributed gate now carries `test_turbulence_sst_wallfn.jl` at n=6 (user): 10/10 at n=2,3 in 3m14s plus 1/1 at n=6 in 1m24s, 4m38s total against the five-minute cap (D101) - why: the gate's own meshes empty no patch below six ranks (D151), so without this entry the class cannot regress visibly; the cap now has 22 s of headroom and the next gate addition must displace something.
- 2026-09-20 D158 the distributed documentation already claimed unsupported models "raise an error rather than silently giving a wrong answer", which was false before D156 - why: nothing called `wrap_eqn` for them and nothing rejected them (D154); the page now also lists the compressible, MRF, density-based, film and multiphase solvers and points at `distributed_ready` as the declared set.
- 2026-09-23 D159 the memory-traffic scaling plan joins P1 as three milestones by mechanism: P1-M28 StructArray mesh layout (+ `cell_nsign` Int8, `face_gDiff` fold), P1-M29 index widths, P1-M30 single-pool threaded Krylov; phase outcome amended; source plan archived to `dev/archive/reviews/p1/memory-scaling-2026-09-23.md` (user) - why: the 8-core motorBike time is DRAM-bound (`B` term) and AoS faces move 128 B per access; StructArray on unchanged kernels measured 16x at 8t on face interpolation (`dev/telemetry/memory_scaling.md`).
- 2026-09-23 D160 the plan's 500-iteration sweeps and full serial suite are split into one command per point or test file, each under the five-minute cap (D101), and run once at milestone close; per-step verdicts use 20-iteration residual comparisons - why: 1t 500 iterations is 163 s alone, a sweep script runs 8 points in one command.
- 2026-09-23 D161 spec gains R13 (storage changes preserve results) and R14 (element access by index is stable) - why: they are the plan's success criteria 1 and 7, outcomes any layout would have to meet, and the only clauses that let a later session refuse a shortcut.
- 2026-09-23 D162 global-ID Int64 decoupling moves from the plan's layout step 3 into P1-M29 with the other width changes; `cell_nsign` Int8 stays in P1-M28 beside the `face_gDiff` fold - why: global IDs are a width change with a distributed blast radius, unrelated to StructArray storage; `cell_nsign` rides the same `.xdm` and type-parameter churn as the gDiff fold.
- 2026-09-23 D163 P1-M28 strict class amended: bitwise at 1t, 2D 1t and MPI n=4; 8t and GPU to at least 8 significant figures; MPI smoke at n=4 not n=8 - why: 8t and GPU reruns of the same revision differ at 10.5 figures while 1t and n=4 reruns are bitwise (`dev/telemetry/memory_scaling.md` § S1); n=8 needs more than the 6.9 GB free beside the language server.
- 2026-09-23 D164 P1-M28 compile band amended from +10% to the measured +25% (motorBike) / +41% (2D kwSST) first-run compile, with attribution, and S2 lands - why: inference rises 1.36x across the same methods with StructArrays internals only 0.68 s, so the cost is the size of every mesh-carrying type, not an avoidable host path (`dev/telemetry/memory_scaling.md` § S2); it is a one-off per process against −13% per 100 iterations at 8t, and the M26 per-case precompile recipe absorbs it for users who need it. REVERSIBLE by the user: the band was theirs.
- 2026-09-23 D165 end-to-end the StructArray layout gives −13% at 8t and −11% at 1t per 100 iterations, not the halving of the DRAM term the plan projected from the isolated kernel (16x at 8t) - why: pinned 100-iteration pairs (`dev/telemetry/memory_scaling.md` § S2); the rest of the 8t time is outside face reads (threaded Krylov vector ops per the diagnosis, cell gathers); S6 refits C/B and profiles before any follow-up.
- 2026-09-23 D166 device-to-host copies in the VTK writers use `adapt(Array, x)` instead of `copyto!` into a plain `Vector`; `_get_backend` reads a plain index array - why: `copyto!` from a device StructArray gathers element by element and trips scalar indexing on the GPU; `KernelAbstractions.get_backend` has no StructArray method, and defining one would be piracy.
- 2026-09-23 D167 the distributed gate now takes 5m04s (n=2,3 part 3m40s, was 3m14s; n=6 wall-function part 1m24s), 4 s over Q2, and every file passes when the n=6 part is run on its own - why: the D164 compile rise lands in every spawned rank process; Q2 is left standing and flagged to the user rather than amended, since the StructArray type size is the lever and S5 removes one mesh type parameter.
- 2026-09-23 D168 P1-M28 restated by the user after S4 (steps landed 4 of 6 declared, compile +51%/+67% vs 875c16bb): measure option B, a thin in-house element container (plan p1-m28 § restatement); if first-run compile is within +10% of the 875c16bb base it is adopted, otherwise option A, the whole mesh as plain column arrays (every `faces[i].x` rewritten, ~250 sites), where short informative accessor functions are allowed if they cost no more than 1-2% runtime against direct column indexing; the provisional D164 band is withdrawn - why: StructArrays' type size costs inference 1.36x and compile rose more than inference, so part is type size (B fixes) and part LLVM on element building (only A fixes) (`dev/telemetry/memory_scaling.md` § S2, S4).
- 2026-09-23 D169 R14 (element access by index is stable) is kept under option B and is amended with a decision under option A, where `mesh.faces[i].area` style access cannot survive unless provided by the accessor layer - why: option A was chosen conditionally by the user (D168) knowing it breaks that idiom.
- 2026-09-23 D170 P1-M28-S7 lands the in-house `FaceArrays`/`CellArrays`/`NodeArrays` as the element storage (StructArrays dependency dropped, bitwise strict class) but is REFUSED as the compile fix: +22% 1t, +28% 2D against a same-session base, identical to S4; an AoS control on the same source equals base, and inference (not LLVM) carries the rise - why: `dev/telemetry/memory_scaling.md` § S7; the type-size half of D168's hypothesis is refuted, and D168's +51%/+67% overstated the gap (base drifted 14.2 → 18.0 s).
- 2026-09-23 D171 P1-M28 grows to 9 declared steps: S9, a lazy element (`faces[i]` returns a view whose field access reads one column), is measured once before option A; it is A's per-access IR behind B's API and counts as D168's permitted accessor layer, so R14 would survive; if it also misses +10% compile, S8 (option A) is next and no fourth container form is tried - why: D170 puts the cost in inference/optimisation of the 8-load element built at every access, which a one-column read removes as A would, at ~30 sites instead of ~250.
- 2026-09-23 D172 P1-M28-S9 (lazy element view) REFUSED and reverted: compile +27% 1t, +33% 2D same-session, no better than S7, and 1t no longer bitwise because a 3D reader read a lazy view after mutating in place; D171's cause (the 8-load element built per access) is refuted since S9 reads one column. Restated mechanism: every container form puts a user-defined `getindex`/`getproperty` at each of the ~400 access sites, whose inlining is the inference rise; AoS access is builtins only - why: `dev/telemetry/memory_scaling.md` § S9, § S7 (AoS control equals base, three column forms at +22-33%).
- 2026-09-23 D173 P1-M28-S8 SUPERSEDED BY P1-M31: option A realised as builtin-only column reads through the S7 containers' fields (`faces.area[fID]`), so `Mesh2`/`Mesh3`, readers, parts and adapt stay as S7 and `mesh.faces[i].area` still works for cold code (R14 kept, D169 moot); hot sites convert first and are screened before the rest - why: D172's mechanism predicts A's gain comes from the access path, not the mesh struct, and 416 subscript sites (64 in BlockMesher2D, 30 gmsh, most readers on plain vectors) make a whole-mesh restructure the costlier route to the same access path.
- 2026-09-23 D174 D172's mechanism (a user method at each access site) and type size are both refuted: AoS behind a wrapper with its own `getindex` compiles at base while its mesh type is as large as S7's, and one bounds check per element leaves S7 at +24-27%; the remaining candidate is the per-access IR of reading several columns (8 loads plus element assembly, or a ref plus one `getproperty` per field) against one struct load - why: `dev/telemetry/memory_scaling.md` § P1-M31 screens; option A's real scope is 416 subscripts plus 122 `@define_boundary` bodies and 12 schemes that take whole elements, so it changes the public BC-definition API, which goes to the user.
- 2026-09-23 D175 the user chose option A in full over accepting the compile cost or reverting to AoS: scheme and boundary-condition functions take the `faces`/`cells` containers plus IDs and read columns; `@define_boundary` binds `face`/`cell` elements only when a body names them, so unconverted and user-written BCs keep working while converted ones read columns; P1-M31 restated to 4 steps - why: user answer to D174's question; the macro binding keeps the BC API change additive rather than breaking.
- 2026-09-23 D176 P1-M31-S1 WITHDRAWN on its screen and option A refuted as a compile fix: converting the Discretise and Calculate hot paths to column reads left compile at +25-28% and those modules' inference deltas unchanged, and an AoS mesh that merely carries the columns unread costs +34-39%; the rise is in every specialisation carrying the column arrays' types (kernel launchers, model constructors), which no access rewrite and no whole-mesh column layout (same arrays) removes - why: `dev/telemetry/memory_scaling.md` § P1-M31 screens; S1 diff kept at `dev/archive/patches/p1-m31-s1-column-reads.diff` (bitwise, not landed); the route goes back to the user.
- 2026-09-23 D177 D176's inference is overturned: the same column arrays held as top-level `Mesh2`/`Mesh3` fields sharing type parameters compile 25-35% FASTER than the AoS base (1t 12.6 vs 16.7 s, 2D 13.9 vs 20.2 s, 8t 18.4 vs 27.6 s) against +33-50% for the S7 nested containers in the same session, bitwise at 1t and 2D, runtime as S7 or better; the cost was the nested container types, not the columns - why: `dev/telemetry/memory_scaling.md` § flat mesh; experiment only, route goes to the user.
- 2026-09-23 D178 the GPU slowdown since P1-M28-S7 (20-iteration run 7.5 vs 4.3 s) is a type-unstable container `adapt`, not the layout: the host copy used by the VTK writer set-up was uninferred and its 1.1M-face loop dispatched dynamically; explicit column adapts with the element type carried over restore parity (4.24/4.11 vs 4.08/4.32 s) in the flat patch; S7 on the branch still carries the bug - why: `dev/telemetry/memory_scaling.md` § flat mesh (CUDA profile: equal GPU busy time, host time in `initialise_writer`).
- 2026-09-23 D179 the user adopts the flat mesh layout (per-field arrays as top-level `Mesh2`/`Mesh3` fields sharing type parameters, `mesh.faces`/`cells`/`nodes` as on-demand views so R14 holds) and orders the discretisation kernels converted to direct array reads as a follow-up; P1-M31 S2-S4 (column reads under nested containers) are withdrawn, S5 lands the flat patch, S6 converts the discretisation path reusing the S1 diff (scheme and BC signatures take the containers, accepted under D175); P1-M28-S5 (gDiff fold) is withdrawn since `face_gDiff` is already a flat column sharing the float parameter - why: D177 (compile 25-35% below AoS, runtime −9-11% at 1t, bitwise) and D178 (GPU parity after the adapt fix); GPU discretise kernels still 5.7/7.4 vs 3.5/4.9 ms per call.
- 2026-09-23 D180 P1-M31-S5 lands the flat mesh layout - why: 1t, 2D, MPI n=4 bitwise, 8t/GPU ≥10.3 figures, compile −24% 1t and −28% 2D vs same-chain AoS base, GPU run parity, 8t pinned 100-iteration −14% vs same-session base (the 16.5 s bar was another day's machine state) (`dev/telemetry/memory_scaling.md` § P1-M31-S5)
- 2026-09-23 D181 the user adds P1-M31-S7 (mesh `get_float`/`get_int` as one-element vectors sharing `VTF`/`VI`, dropping two mesh type parameters) ahead of S6; plan exceeds its restated two steps by a user-requested row inside the milestone's outcome, so no split - why: user request 2026-09-23; both fields only feed `_get_float`/`_get_int`
- 2026-09-24 D182 P1-M31-S7 lands: mesh `get_float`/`get_int` are one-element arrays sharing `VTF`/`VI` (mesh 9 → 7 type parameters), passed through only when already of the sibling field's array type, part format unchanged; per-field comments and the `Mesh3` docstring restored - why: 1t, 2D, MPI n=4 bitwise, GPU 11.3 figures, compile unchanged vs S5; first form passed any `AbstractVector` through and silently stored `SVector`/`UnitRange` tags as 3- and 2-element vectors, caught by `test_offline.jl` (`dev/telemetry/memory_scaling.md` § P1-M31-S7)
- 2026-09-24 D183 `test_restart.jl` runs under `dev/petscenv_stock`, not `dev/petscenv` - why: in `dev/petscenv` it errors with no method `MatMPIAIJGetSeqAIJ` at `_split_matrix`, independent of the mesh; the same source passes at n=1,2 under the stock env
- 2026-09-24 D184 amends D180: the flat mesh layout slowed the GPU discretise kernels per call from 3.50/4.95 ms (scalar/vector, AoS base) to 5.87/7.60 ms at S7, unseen by S5's run_s parity check because those kernels are ~1% of GPU run_s - why: `gpu_profile.jl` at S7 HEAD vs `env_base`, same session (`~/.cache/xcal_m28/s6/prof_*.prof`)
- 2026-09-24 D185 P1-M31-S6 lands (discretisation, Calculate and BCs read mesh columns; `scheme!`/BC take `cells, faces`) with its GPU bar restated from AoS base to S7 HEAD, since the base gap is the flat layout's argument cost (D184) that S6's mechanism cannot reach - why: per call scalar 5.90 vs 5.87 ms, vector 6.90 vs 7.60 ms at HEAD; 1t, 2D, MPI n=4 bitwise, 14 suite files pass, compile within S5 noise (`dev/telemetry/memory_scaling.md` § P1-M31-S6)
- 2026-09-24 D186 opens P1-M31-S8, a kernel argument diet, carrying the original ≤ AoS-base discretise bar - why: PTX `__local_depot` per thread 3984/3480 B flat vs 2304/1944 B base with equal parameter counts, so by-value argument structs (mesh and field meshes) drive the per-call slowdown
- 2026-09-24 D187 P1-M31-S8 lands: discretise kernels take mesh-free terms, sources, `prev` and `rho_prev` (phi keeps only `face_gDiff`) plus the cell/face columns they read, instead of `model` and `mesh`; `VectorField` mesh parameter unconstrained so a mesh-free view exists; `bounding_box` reads its backend from `face_nodes` (a `FaceArrays` view has none, broken since S5) - why: PTX depot 408/880 B vs AoS base 1944/2304, per call 0.66/1.73 ms vs base 3.50/4.94, 1t, 2D, MPI n=4 bitwise, GPU 10.6 figures, compile −7-10% vs S6, 12 suite files pass (`dev/telemetry/memory_scaling.md` § P1-M31-S8)
- 2026-09-24 D188 P1-M31 closes as delivered: S5, S6, S7, S8 landed on their bars and every exit clause is met (compile −22-35% vs AoS base, strict class, GPU run below base, discretise kernels 2.9-5.3x faster than base per call) - why: D180, D182, D185, D187
- 2026-09-24 D189 P1-M28-S6 closes P1-M28 as delivered: every objective met at 500 iterations (8-rank MPI 53.6 vs 69.7 s, 8t 61.4 vs 78.0, GPU 17.2 vs 22.4, 1t 135.6 vs 162.9), B −24%/−26% at unchanged C, isolated face interpolation 7.5x and cell gather 7.1x at 8t; the conditional follow-ups (face-based assembly, extra face coefficients) are not opened since no kernel scales below 4x; GPU workgroup 32 stays - why: `dev/telemetry/memory_scaling.md` § P1-M28-S6 close, full serial suite and distributed gate green
- 2026-09-24 D190 P1-M30-S1 is delivered by the M28 close profile (100-iteration 8t main thread): serial Krylov vector work ~22% of main-thread samples, `wait` 50%, and string building for progress output ~8%, a lever M30's mechanism does not reach - why: `~/.cache/xcal_m28/close/profile_close_8t.txt`, same measurement S1 prescribes
- 2026-09-24 D191 P1-M29-S1 lands: global ids (`local_to_global`, `orig_cells`, `orig_faces`, `cellProcAddressing` reads) are `GlobalInt = Int64` with their own type parameter on `Partition` and `DistributedMesh`, owners and every local index stay `TI`; part format 5 writes them as Int64 - why: MPI n=4 on fresh parts bitwise vs `m28_baseline`; offline/partition/io 6/6 incl. an Int32 part with ids above `typemax(Int32)` round-tripping the file, gate 8/8 + n=6 1/1, repartition 2/2, restart 3/3 (`dev/petscenv_stock`)
- 2026-09-24 D192 P1-M29-S2 lands: the AMG hierarchy stores rowptr/colval, `diagonal_index`, `aggregate_ids`, the RAP plan and the pattern signature in the equation matrix's index type, passed to `_workspace(solver, b, _index_type(A))` at every setup site because the workspace's concrete hierarchy type is fixed before the matrix is seen; setup-only coarsening scratch and the SuiteSparse CSC boundary (`csc_nzval_index`, coarse LU) stay `Int` since they move no bytes per cycle - why: motorBike Int32 AMG 20 iterations bitwise vs pre-S2 same session, run_s 13.80 vs 14.42 s; `test_AMG.jl` (new Int32 hierarchy test), `test_AMG_matrices.jl` and 9 touched-module suite files pass; GPU AMG smoke runs
- 2026-09-24 D193 P1-M29-S3 lands: periodic `face_map`, `faceAddress1/2` and the periodic matrix triplets take the mesh index type; before, the `Int` triplets promoted an Int32 periodic mesh's equation matrix to Int64 (probe: `SparseXCSR{…,Int64}` pre, `Int32` post); the `src/` sweep finds no other `Int` array stored on a per-iteration path (partition index, writer order, halo rank ids, setFields and reader scratch are setup or I/O only) - why: cascade periodic suite file with a new Int32 matrix test, `unit_test_laplace.jl` and distributed `test_periodic.jl` n=2,3 pass
- 2026-09-24 D194 P1-M29-S4 lands: `UNV2D_mesh`, `UNV3D_mesh`, `FOAM3D_mesh`, `FOAMCase` default to `integer_type=Int32`; a checked integer conversion failing during an Int32 build is rethrown as an `ArgumentError` asking for `Int64`, and the equation builders check the matrix entry count before `sparsecsr`; the capacity check is per read (the global mesh for `distribute(mesh)`, one part per rank for `FOAMCase`); bar restated before the run: default == explicit Int32, explicit Int64 unchanged, so the smoke's 2d mode pins `Int64` - why: 45 serial suite files pass, 2d (Int64) and motorBike 1t (Int32) bitwise vs `m28_baseline`, gate 10/10 + n=6 1/1, docs build green
- 2026-09-24 D195 P1-M29 closes as delivered: global ids Int64 (D191), AMG (D192) and periodic (D193) indices follow the mesh type, Int32 the reader default with a capacity error (D194); exit criterion met (residuals bitwise, Int32 part with ids above `typemax(Int32)` round-trips, suite green by file) - why: D191-D194
- 2026-09-24 D196 P1-M30-S2 lands: internal `XVector{T} <: DenseVector{T}` in `src/Multithread/xvector.jl` wrapping a `Vector`, chunked as `xmul!` with chunk c on thread c (`@threads :static`), serial at one thread; Krylov.jl's `kdot`, `knorm`, `kscal!`, `kcopy!`, `kscalcopy!`, `kdivcopy!`, `kaxpy!`, `kaxpby!`, `kfill!` threaded over it, reductions from cache-line-padded per-chunk partials summed in chunk order; not wired into any solve yet - why: Krylov 0.10.10 source builds workspaces with `similar(b)` and `ktypeof` keeps a `DenseVector` type, so an `XVector` right-hand side makes every workspace vector an `XVector`; `unit_test_xvector.jl` passes at 1 and 8 threads incl. a CG solve on an `XVector` workspace
- 2026-09-25 D197 P1-M30-S3 lands: equation workspaces hold `XVector` storage on the CPU, `solve_system!` passes `b`/`values` in the workspace's storage type, `xmul!` and the CPU Jacobi apply share its static chunks, and a call touching fewer than 2^16 elements stays serial; R13 and the M30 strict bar move from a flat 8 figures to 'no more than the revision differs from itself between two thread counts' - why: motorBike 8t −13%, 1t equal, ≥10.2 figures; 2D SST 7.6 figures vs baseline equals its own 1-vs-2-thread spread; unthresholded threading doubled a small transient case (`dev/telemetry/memory_scaling.md` § P1-M30-S3)
- 2026-09-25 D198 P1-M30-S4 lands: `activate_multithread` defaults BLAS back to one thread (keyword kept), docstring rewritten; the unreleased CHANGELOG/release-notes entry that raised the default is removed, so nothing changes against v0.6.0; the benchmark README BLAS section is left to the user because that repository carries uncommitted user edits - why: 8t smoke 4.83 s vs 4.85 s at S3, 1t 9.64 s bitwise vs S3 (`~/.cache/xcal_m28/m30s4/`)
- 2026-09-25 D199 P1-M30-S5 lands: `xmul!(A, x)` (undefined `y`) and `xmul(y, A, x)` (discarded `y`) removed; `Base.:*(::SparseMatrixCSR, ::SparseXCSR)` (a missing 2-arg `xmul`) replaced by a correct threaded `A*x` for `SparseXCSR` - why: no caller in src, ext, test, docs or examples; `unit_test_xvector.jl` (new product test) and `unit_test_laplace.jl` pass at 1 and 8 threads
- 2026-09-25 D200 P1-M30-S6 closes P1-M30 as delivered: 500 iterations 8t 48.1 s (M28 close 61.4), 11% below 8-rank MPI 54.1 s, 1t 137.6 s (+1.4%, noise); main-thread Krylov vector primitives ≈1.6 s per 100 profiled iterations, under 3 s, so no fused CG and no persistent thread team; progress-output string building (~8% of main-thread samples) is outside M30's mechanism and left for a later milestone - why: `dev/telemetry/memory_scaling.md` § P1-M30-S6 close; full serial suite 46/46 at 4 threads
- 2026-09-25 D201 the user opens P1-M32: a `progress` keyword on `run!` (default `true`) that turns progress output off, recommended off for large-scale runs that are not on a local PC - why: progress-output string building is ~8% of 8t main-thread samples (D200)
- 2026-09-25 D202 amends D200/D201: the ~8% main-thread string building is VTK `initialise_writer` (a per-`run!` setup cost built even with `write_interval=-1`, ~3 s on motorBike), not progress output; P1-M32's exit drops the profile clause and keeps the keyword as a display switch - why: profile tree of `profile_m30_8t.jlprof`, every string frame's nearest XCALibre caller is `VTK_writer_3D.jl:49-132` under `SIMPLE.jl:144`
- 2026-09-25 D203 P1-M32 lands and closes: `run!(...; progress=true)` on every solver (`_progress_bar` builds no display when off or distributed), documented with the recommendation for large-scale runs not on a local PC; the serial test suite passes `progress=false` (user request) except one on/off comparison - why: motorBike 1t bitwise on vs off, 12 suite files over every solver family, distributed PSIMPLE/Laplace 4/4, docs build, edited tests pass with no progress output
- 2026-09-24 D204 the user opens P1-M33: a quick investigation of the distributed path for easy performance wins, starting with whether PETSc and Krylov.jl stopping criteria are comparable - why: 8-rank MPI 54.1 s now trails 8 threads 48.1 s (D200)
- 2026-09-24 D205 P1-M33 closes as delivered: PETSc and Krylov.jl stopping tests are equivalent (same norms, rtol on the warm-start initial residual) and give identical iteration counts per equation, so the MPI path does no extra work; `KSPSolve` is 52% of rank 0; the one easy win is a 32-bit-index PETSc (−7.6% at n=4, bitwise), which the current PETSc_jll 3.25.4 does not ship and 3.22.2 does; no source change made; `-log_view` gave nothing because the global PETSc log is never started, so a rank-0 Julia profile replaced it - why: `dev/telemetry/memory_scaling.md` § P1-M33
- 2026-09-24 D206 `A0` stays in both paths: it is the discretisation target and the per-component reset source, and the reset is what keeps periodic BCs correct, since they add (`+=`) component-independent off-diagonal and partner-diagonal terms at every application; the distributed path already mirrors the serial A0 handling; the only extra it has is the full value copy into PETSc for U's y and z solves, which a diagonal-only write into the host `_SplitFill` arrays could replace (COO/GPU path keeps the full copy); worth ~1% of rank 0 (`passemble!` 3.6% over 6 copies per iteration); not built pending the user - why: `Discretise_5_apply_bcs.jl` writes only diagonal and b, `periodic.jl:304-443` writes NP/PN off-diagonals, `_A0` read only by `discretise!` and `update_equation!`
- 2026-09-24 D207 REFUSED with the user: diagonal-only PETSc writes for U's y and z solves (D206) are not built; ~1% of rank 0 does not pay for a CPU-only second assembly behaviour (the GPU COO path would keep full writes) resting on an unenforced invariant (component matrices share off-diagonals) that a future component-coupling BC would break silently - why: user decision 2026-09-24 on the D206 estimate
- 2026-09-24 D208 pre-merge review recorded (`dev/archive/reviews/p1/pre-merge-review-2026-09-24.md`); A1 (`@threads :static` crash when `run!` runs inside another threaded region) re-confirmed; D198's claim that the BLAS-default CHANGELOG line was unreleased is wrong, origin/main #161 carries it (B4) - why: three Opus reviews at c0ef5101; nested `kdot` on a 200k XVector at `-t 4` throws '`@threads :static` cannot be used concurrently or nested'
- 2026-09-24 D209 the user opens P1-M34 (robustness, A1-A5 with A5 folded in), P1-M35 (behaviour changes and docs, B1-B7) and P1-M36 (cheap per-iteration wins C1, C2, C5, C6 and the D202 writer setup); M34 and M35 block the merge, M36 does not; C3, C4, C7-C9 stay recorded in the review, unscheduled - why: user decision on `dev/archive/reviews/p1/pre-merge-review-2026-09-24.md`
- 2026-09-24 D210 P1-M34-S1 lands: chunked Krylov primitives run `@threads :static` only outside a threaded region (with a fallback when Base's check races a concurrent loop), else `:dynamic` over the same chunks; AutoTune workgroup `cld(max(n,1), nthreads)`; repros in `unit_test_xvector.jl` failed before (`:static` nested error, `min_elems must be > 0`) - why: tests pass at `-t 1`, `4`, `4,1`; motorBike 1t residuals bitwise vs `m30s4`, 8t 13 figures, run_s 9.20/4.75 s within noise (`~/.cache/xcal_m28/m34s1/`)
- 2026-09-24 D211 P1-M34-S2 lands: `_on_all_ranks` (try, all-gather of a failed flag, failing ranks rethrow and the rest error naming them) wraps rank-0 read/partition in `distribute(mesh)`, `distribute(reader)` with and without `dir` (replacing its barrier), the per-rank part read, the per-rank FOAMCase read plus a new `cellProcAddressing` length check, and restart field reads (syncs after); `driver.jl` kills a file after `timeout=1200` s so a hang fails; `test_failure.jl` joins `gate.jl`; not covered: a throw in rank 0's send loop or in `_attach_ghosts` - why: `test_failure.jl` 2/2 at n=2,3 with each rank printing the intended error; `test_offline` 2,3, `test_io` 2, `test_restart` 1,2,4 (petscenv_stock) pass
