# Active phase roadmap

phase: P1 - distributed module release polish

## Outcome

The distributed module becomes an ordinary, documented XCALibre feature: it installs and runs on stock binaries, is configured only through the Julia interface, cannot be set up rank-divergently, carries a fast regression gate, has its parallel efficiency attributed to a measured cause, and costs serial users nothing. Serves R1 through R10. Amended by D72: the module is also structurally ready to scale before HPC validation (P2): no rank holds the global mesh or does O(P·N) work, the linear system exists once per rank, and communication is fused and overlapped (R11, R12).

## Ordered work

- [x] **P1-M1 - vault and record reconciliation** The development record is one tracked, unmixed vault; `dev/` leaves `.gitignore` while machine-specific state stays out of Git; the working backward-facing-step example fix lands. Exit: `xcalibre-dev check` valid, `dev/` tracked, example committed and pushed. Expected 3 steps.
- [x] **P1-M2 - fast distributed gate** The distributed suite runs from the repository's prescribed test command in a subset chosen to meet Q2, with the rank count passed as a test argument rather than an environment variable. Exit: gate green from `Pkg.test`, timed, recorded in telemetry. Serves R9, R2. DELIVERED: five files at two ranks, 5/5 in 89 s against the 300 s bar.
- [x] **P1-M3 - scaling attribution** The parallel efficiency loss is split across named stages on the backward-facing-step case at Float64 on CPU with a partition-invariant pressure preconditioner, so that the cause is measured rather than listed. Exit: telemetry naming the dominant cost and the rank-invariance check. Serves R7, R8, Q1, Q3. DELIVERED, then REOPENED and CORRECTED: the first attribution blamed memory bandwidth and was wrong (D16 withdrawn by D19). With the CPU clock pinned, efficiency is 102/94/71% at n=2/4/8 and is unchanged by a 2.6x larger mesh (D20, D21); the apparent loss was this laptop throttling 4400 to 3100 MHz as rank count rose. XCALibre ties OpenFOAM's production GAMG per iteration using only Jacobi and scales better at every rank count (D22). Residuals agree to 15 significant figures at every rank count. The residual above four ranks is `VecNorm` reduction cost and is deferred (D23). Telemetry: `dev/telemetry/scaling_attribution.md`.
- [x] **P1-M4 - distributed setup interface** One rank-uniform way to obtain a distributed mesh, a root guard for output, options that were environment variables moved onto the interface, and a single documented launch command. Exit: the example reads as a serial case plus mesh and launch; no environment variable is required. Serves R1, R2, R3, R5, R6. DELIVERED in five steps. Plan: `dev/archive/plans/p1/p1-m4-distributed-setup-interface.md`.
- [x] **P1-M5 - distributed dependencies become optional** Decide, on a measurement rather than a principle, whether MPI and Metis stay hard dependencies. The cost to a serial user is measured first: install size, precompilation and load time. Only if that cost is material does the module's MPI surface move behind an extension. Exit: the measurement recorded and the decision taken, plus the refactor if it is warranted. Serves R4. CLOSED WITHOUT THE REFACTOR: the cost is 0.17 s of an 0.809 s load and 27 MB, so the dependencies stay and R4 is amended to a bounded measured cost (D18).
- [x] **P1-M6 - documentation and messages** A distributed user-documentation page, a CHANGELOG entry for the feature, corrected diagnostic messages that point at the documentation, and the documented scope of what is and is not supported. Exit: documentation builds, scope matches the spec's non-requirements. Serves R10. DELIVERED: the distributed page now covers stock-binary requirements, the rank-uniform mesh call, `mpiexecjl`, the supported and unsupported scope, and what to expect of parallel performance; the CHANGELOG records the feature; the hypre and GPU-solve errors point at the documentation.

- [x] **P1-M7 - preconditioner API, informed by the scaling measurements** The distributed preconditioner API says what it does to the hierarchy, and the documented default is the one that wins. Delivered: `reuse` renamed `freeze` (D38, D41), GAMG default freeze 25 with BoomerAMG kept at 10 (D39), GAMG recommended with the rank-invariance caveat documented (D40). Plan: `dev/archive/plans/p1/p1-m7-preconditioner-api.md`. Exit: a measured recommendation for the distributed pressure preconditioner, the naming settled, and the docstrings matching the semantics. Serves R5, R10. Expected 4 steps.
- [x] **P1-M8 - PETSc solver interface polish** `convergence` means the same thing on every path; the PETSc mapping covers every serial solver and preconditioner type a user script can name, with the options passthrough reachable for the rest; single-process PETSc solves are documented; the distributed page is generic and its API examples run as doctests. Exit: distributed gate green, docs build with the new doctests green, serial single-process PETSc run recorded. Serves R5, R10. Expected 4 steps: S1 `convergence` reconciliation, S2 mapping catalogue, S3 docs rewrite + doctests, S4 single-process PETSc documented. DELIVERED in one diff (D42-D46): gate 5/5, docs build with doctests green.

- [x] **P1-M9 - GPU solves require a GPU-enabled PETSc** The host-copy opt-in is removed: device fields with a PETSc build lacking the device package always error, pointing at the documentation. Also warns once when `DILU` is mapped to block ILU(0) (D50). Exit: no host-staging option on the interface, error message, DILU warning and docs updated, distributed gate green. Serves R10. Expected 1 step (D47, D50). DELIVERED: gate 5/5 in 99.7 s; GPU cavity n=1,2 green on the CUDA PETSc.
- [x] **P1-M10 - device-resident PETSc solve** Matrix values, right-hand side and solution move between XCALibre and a GPU-enabled PETSc without a host round trip per solve. Exit: a CUDA-enabled PETSc build in a project environment, the device path run on this machine's GPU with residuals matching the CPU path, and the docs stating what is verified. Serves R10. Expected 3 steps (D47). DELIVERED in one step (D51, D52): GPU 25% faster per iteration, CPU unchanged, residuals unchanged; AMD GPU solves now error (D52).
- [x] **P1-M11 - reduction count above four ranks** Cut the per-outer-iteration global reductions that dominate the eight-rank solve (D23). Exit: reductions per outer iteration counted before and after, eight-rank efficiency measured with the clock pinned, rank invariance kept. Serves Q3. Expected 3 steps (D48). S1 LANDED (D53-D55): tolerances now match serial Krylov.jl (distributed BFS residuals at 30 iterations 10-30x lower, now tracking serial), CG reductions per iteration ~3.1 to ~2.1, but n=2 to n=8 speedup unchanged (2.88 to 2.83), so the count is not the cause; S2 refused single-reduction CG (D58); S3 placed the n=8 wait inside the PETSc solve, with balanced work under -log_sync (D59). DELIVERED at that: the count was cut and measured, the loss is not the count; the byte-traffic mechanism moves to P1-M14 (D60).
- [-] **P1-M12 - AMG preconditioner questions** - SUPERSEDED BY P1-M13 (D56)
- [x] **P1-M13 - preconditioner guidance and defaults** BoomerAMG, GAMG and Jacobi all stay exposed. Measure what each does to the distributed pressure solve and where it helps and does not (mesh size, stiffness, rank count, CPU/GPU), including how the `freeze` optimum moves with mesh size, and turn that into user documentation and defaults. Exit: telemetry covering at least two mesh sizes and two rank counts, a documented description per preconditioner stating when to use it and when not, defaults chosen from the measurement. Serves R5, R10. Expected 5 steps (D56, D57, D62). Plan: `dev/archive/plans/p1/p1-m13-preconditioner-guidance.md`. DELIVERED (D63-D66): BoomerAMG P_max=4, IC0GPU/ILU0GPU mapped, per-equation petsc_options, a choice table plus GPU, memory and recipe guidance in the docs; defaults unchanged on measurement.
- [x] **P1-M14 - PETSc index width** Use the 32-bit-index PETSc library whenever the global system fits it, since `MatMult` is memory-bound and 64-bit indices move a third more bytes per nonzero. Exit: selection rank-uniform and automatic, per-iteration time measured at n=2 and n=8 against the 64-bit library, residuals unchanged, suite green. Serves R7, Q3. Expected 1 step (D60). DELIVERED (D61): 5% faster at n=2, 12% at n=8, residuals bitwise identical, suite 12/12.
- [x] **P1-M17 - MPI and PETSc setup guide** The distributed page gains a setup section: stock binaries (no configuration), MPI.jl with a system or CUDA-aware MPI (MPIPreferences, MPItrampoline), PETSc.jl pointed at a custom PETSc (preferences, one project environment per build), and the least painful route to a CUDA-enabled PETSc. It states plainly what a CUDA-aware MPI alone gives with the stock PETSc_jll (GPU-aware halo exchange, but no GPU solves, since PETSc's CUDA support is fixed when PETSc is compiled) and whether a prebuilt CUDA PETSc can be loaded, so users avoid compiling. Also fix the run-on paragraph in "When AMG helps". Exit: every procedure in the section run once on this machine or cited to its source where hardware is missing, docs build green. Serves R1, R6, R10. Expected 2 steps (D68). Plan: `dev/archive/plans/p1/p1-m17-setup-guide.md`. DELIVERED (D69-D71): setup section with conda-forge CUDA PETSc as the no-compile GPU route, verified at n=1,2 with Open MPI and MPICH; GPU communication path auto-detected (direct or host-staged, reported once) so no env var is needed; gate 5/5, docs build green.
- [x] **P1-M18 - release blockers** DELIVERED 2026-09-19 (D78-D85; the segfault was PETSc's CUDA device init resetting Julia's signal handlers, D85). Plan: `dev/archive/plans/p1/p1-m18-release-blockers.md`. The eight blockers in `dev/archive/reviews/p1/audit-2026-09-18.md` that need no new hardware: stray result files off the branch, BoomerAMG on device fields errors instead of segfaulting, VTK on a distributed mesh errors instead of writing nothing, partition files carry a checked header, the communicator is stored on the mesh and used everywhere, device binding by node-local rank, guarded `use_gpu_aware_mpi` write, cached reference cell, Julia 1.10 floor recorded, CUDA-aware segfault root-caused under `compute-sanitizer`. Exit: distributed suite green at n=1,2 (stock) and `test_gpu.jl` at n=1,2 (conda env), `git diff main --stat` free of result files. Serves R1, R2, R10. Expected 11 steps. Plan: `dev/archive/plans/p1/p1-m18-release-blockers.md`.
- [ ] **P1-M19 - gate widening** The gate runs at n=2 and n=3, the full suite at 1, 2, 3, 5 and an oversubscribed 8; a ghost-consistency assertion runs after every self-syncing primitive; rank invariance at n=1,2,4 is a test; exchange and all-reduce counts per iteration are budgeted in `test_perf.jl`. Exit: gate green within Q2 at both counts, the four new tests green, counts recorded. Serves R8, R9, Q1, Q2. Expected 4 steps. Plan: `dev/plans/p1-m19-gate-widening.md`.
- [ ] **P1-M20 - O(N+P) extraction** `partition_cells`, `build_dual_graph` and `extract_subdomain` cost O(N+P) in total instead of O(P·N): cells bucketed per part once, inverse maps as reset-on-exit vectors instead of `Dict`s, the dual graph built from `cell_neighbours` without triplets. Exit: parts bitwise identical to today's, wall time and peak RSS of `partition_mesh` recorded before and after at 5 mm (P=8, 64) and 4 mm (P=8). Serves R11. Expected 3 steps. Plan: `dev/plans/p1-m20-extraction.md`.
- [ ] **P1-M16 - memory breakdown** RESTATED by D76: measurement only. One telemetry table per rank over at least two mesh sizes (5 mm and 4 mm BFS) at n=2, splitting peak RSS and `Base.gc_live_bytes()` into Julia runtime, mesh, fields, equation matrices and work arrays, gradients and turbulence, halo, PETSc (`-memory_view`, `-malloc_view`), setup transient versus steady state. Evidence to start from: 2.79/2.67 GB per rank at 660k cells (`dev/telemetry/preconditioner_guidance.md`), 1.6 KB/cell rank-0 mesh (`dev/gotchas.md`), 1.5-1.8 KB/cell live by struct layout (`dev/archive/reviews/p1/audit-2026-09-18.md`). Cures live in P1-M21 and the D67 PR. Exit: the table recorded in `dev/telemetry/memory_breakdown.md`. Serves R11. Expected 1 step.
- [ ] **P1-M21 - owned-row single-copy linear system** Each rank's CSR holds owned rows only through the `n_rows` seam (D74), PETSc wraps the owned prefix of `x` and `b` zero-copy, the COO map is kept or replaced on a measured footprint, `GC.gc(true)` after setup and a verified per-rank heap recipe. Exit: peak RSS per rank at 5 mm and 4 mm n=2 recorded against M16's table with residuals bitwise identical under Jacobi and per-iteration time within a 3 percent band; shared-code cures listed for the D67 PR. Serves R7, R11. Expected 6 steps. Plan: `dev/plans/p1-m21-owned-row-system.md`.
- [ ] **P1-M22 - fused, overlapped, stream-aware communication** One halo schedule per mesh shared by every equation with distinct tags; one width-3 exchange for the three momentum components with residuals deferred; one exchange for `rD` and `Hv`; one all-reduce per iteration for all residuals; a `sync_begin!`/`sync_end!` pair measured at two body points; GPU handoff to PETSc by stream events instead of device drains, and one stream sync per halo. Exit: rounds and all-reduces per iteration counted before and after, n=8 pinned per-iteration time and GPU n=1 time recorded, residuals bitwise identical under Jacobi. Serves R7, Q3. Expected 7 steps. Plan: `dev/plans/p1-m22-communication.md`.
- [ ] **P1-M23 - parallel preprocessing** A binary part format with a header replaces `.jls`; a decomposed-OpenFOAM reader builds each rank's `DistributedMesh` from its own `processor<rank>/` directory with ghosts exchanged over the processor patches, so no rank reads the global mesh; a parallel repartition of any `DistributedMesh` through PETSc's `MatPartitioning` where the build has ParMETIS or PT-Scotch. Exit: `decomposePar` output of the BFS runs at n=2,4 matching serial to the `test_psimple.jl` bar and round-trips through `reconstructPar`; part-format load time recorded. Serves R11. Expected 5 steps. Plan: `dev/plans/p1-m23-parallel-preprocessing.md`.
- [ ] **P1-M24 - binary output and restart** The decomposed writer emits OpenFOAM binary, writes the face flux, and a `read_fields!` restores every field a restart needs, so a run resumed at iteration k reproduces the uninterrupted run. Exit: restart test green (resume at 50 of 100 equals straight 100 to 1e-10 on the p residual under Jacobi), write time at 4 mm n=2 recorded. Serves R12. Expected 3 steps. Plan: `dev/plans/p1-m24-io-restart.md`.
- [ ] **P1-M15 - example comment blocks** Every comment block in `examples/*.jl` is at most three lines, as the vault check requires; the known offenders are mostly commented-out plotting and profiling code plus a few narrative headers (2D_BFS*, 2D_bump_NASA_kwSST, 2D_cavity, 2D_cylinder and others). Commented-out code is deleted or becomes live optional code; narrative becomes at most three lines. What each example computes does not change. Exit: `xcalibre-dev check` VALID (re-run it: its list may be capped), and every edited example still parses. Serves R10. Expected 1 step (D67).

## Exit gate

PASSED 2026-09-18 for M1-M6, at commit 2374a1e7. M7 closed 2026-09-18 on serial suite 1555/1555 (incl. distributed gate 5/5) and documentation build 0 errors; `dev/telemetry/gate_results.md`. Evidence in `dev/telemetry/gate_results.md`: serial suite 1549/1549, distributed gate 5/5 in 187 s, documentation build 0 errors, example at n=2 and n=4 on stock binaries from clean directories, rank invariance to 15 significant figures under Jacobi, scaling telemetry in `scaling_attribution.md`.

Full serial suite green with no reduction in test count, distributed gate green within Q2, the backward-facing-step example run on stock binaries from a clean checkout, rank invariance at one, two and four ranks meeting Q1, scaling telemetry meeting Q3, and the documentation build passing.
