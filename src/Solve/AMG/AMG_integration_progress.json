{
 "_index": {
  "U1": "mechanical move/rename, no logic change (Sonnet-delegable)",
  "U2": "dispatch unification + MatrixFreeHierarchy struct merge",
  "U3": "delete spikes, consolidate validation, comment cleanup (Sonnet-delegable)",
  "U4": "new tests T1-T3 + full acceptance + record results"
 },
 "tasks": [
  {"id": "U1", "desc": "Move to new layout + rename table + AMG.jl includes + test symbols", "done": true, "note": "whole-file moves only (validation->10 + spike deletion deferred to U3); device/0,1,2,3,6 spikes stay included, renamed in place; tests 213+194 identical; F1 GPU harness deferred to U2 (rename-only, ext disjoint)"},
  {"id": "U2", "desc": "amg_hierarchy_kind, workspace-kind selection, absorb MFGreenfield, delete gate+Ref", "done": true, "note": "AbstractAMGHierarchy supertype; AMGHierarchy+MatrixFreeHierarchy subtype it. workspace.hierarchy IS the MatrixFreeHierarchy on the mf path (no greenfield Ref). MFGreenfield absorbed: MatrixFreeHierarchy gained refresh_plan/cell_perm_device/residual_permuted/nrows/nnz/is_symmetric/last_cycle_factor/workgroup; coarse_fac/coarse_inv/refresh_plan now Ref{Any} so empty==built type. update!->_amg_update!(hierarchy,...) dispatch; outer_operator(h,A) methods; device/0 deleted. Shared solve-loop helpers widened AMGHierarchy->AbstractAMGHierarchy (3,4,5,6 + ext _matvec!/_residual! SPARSEGPU). device_array refactor SKIPPED (closures work, pure churn). GATE PASS on CUDA: F64 ref45==mf45, F32 ref31==mf31, both converged, mf_routed=true."},
  {"id": "U3", "desc": "Delete device/0,2,3,6 + macro-layout leftovers; 10_AMG_validation.jl; comment policy", "done": true, "note": "device/ dir GONE (1,2,3,6 deleted; 0 was U2). Production keepers relocated to top of file 7: aggregate_permutation, permute_operator, _amg_index_type, transfer_factors, _fill_device! (all were in deleted device files, used by 7/8). Validation drivers consolidated into NEW 10_AMG_validation.jl (loads last): host_csr_matvec+host_jacobi_sweep!+host_reference_vcycle+validate_matrix_free_cycle/convergence+measure_cycle_allocations+validate_fused_operator (from 7) + host_frozen_rap_oracle+validate_refresh+validate_refresh_convergence (from 8) + compare_amg_paths (from 9). Spike funcs DELETED (no prod refs): AMGMacroLayout/build_macro_layout/macro_layout_spmv_error/refresh_macro_values!, _amg_local_index_type/_amg_fused_workgroup/_max_aggregate_size, all fused_2grid_* + MF2GridState/mf_2grid_cycle, g3_block_smoother_gate + _host_pcg/_host_ml_vcycle_block/_host_block_jacobi_sweep!. Tests: removed 5 spike testsets (macro_layout, fused index types extras, fused 2grid correction, g3 gate, fused 2grid cycle); kept _amg_index_type test. Comment cleanup: trimmed narrative headers ONLY in files touched (7 header, 10 new, edited tails); files 0-6 + 8/9 bodies left as-is (deferred to U4 if wanted). GATE PASS: CPU 360 pass/0 fail (was 407 w/ spikes); precompile clean; F1 CUDA F64 mf45==ref45, F32 mf31==ref31, both routed+converged."},
  {"id": "U4", "desc": "Tests T1(F32 512^2), T2(CPU ignores fl), T3(GPU fl0==fl1); acceptance vs baseline", "done": true, "note": "T1+T2 in test_AMG_matrices.jl (@testset), T3 in test_AMG.jl CUDA block. GATE GREEN: both files 0 fail/0 error with CUDA functional; T1 4/4, T2 3/3, T3 ran (8 CUDABackend logs, fl1+fl2 mat-free). T1 asserts solver-reported convergence not true residual (F32 true res floors ~4e-3=eps_f32*cond; anchor 3.7e-6 = recurrence rel; iters=17 matches Geo ml2). T1 Array(A) densify->OOM avoided. T3 uses screened Poisson (diag=neighbors+1) bc pure graph-Laplacian diverges under omega=4/3 Jacobi+Geometric / Cg rz<=0 bail; fl0/1/2 all 7 iters, mat-free vs materialised sol agree 1.7e-16, fl2 trueres 2.48e-9. Messaging unification also done (shared _amg_log_hierarchy). NOTE: messaging + tests staged NOT committed. F1 acceptance wall-time + 50-iter SIMPLE smoke NOT re-run this session (unchanged solver numerics; mf/ref 45/45 still holds from U2/U3 + messaging gate)."}
 ]
}
